# AI 资料迁移与本地同步工具

日期：2026-08-26。

## 已执行

1. 保留 `raw/docs/ai` 的 14 篇原文不变，避免破坏历史引用。
2. 将 14 篇文档完整复制到 `raw/知识/通用` 或相应项目目录；映射清单见 `raw/知识/通用/来源记录/2026-08-26_AI实战资料分类清单.json`。
3. 新建项目实战案例索引，按功能主题关联 IslandAuctionKing、pet_paradise、DanDuan 的案例与通用参考。
4. 改建本地同步工具。在 `tools/` 下新建 `Sync-OasisDocs.ps1`，并在仓库根目录下新建方便双击运行的 `双击运行本地同步.cmd`，其内部在 `D:\oasis-skill-plus` 运行 `node src/cli.mjs sync-all`，成功后先清理、再完整替换 `raw/docs/api` 与 `raw/docs/wiki` 镜像目录。

## 2026-08-28 缺失内容修复

对照 `D:\oasis-skill-plus\docs` 与知识库镜像发现，API 镜像完整，但 Wiki 镜像曾只有 7 个 Markdown 和 1569 张图片，参考仓库当时已有 320 个 Markdown 和 3385 张图片。根因是镜像目录没有在复制结束后做源/目标完整性核对，导致不完整镜像未被发现。

本次在 `tools/Sync-OasisDocs.ps1` 增加 `Get-FileManifest` 与 `Assert-MirrorComplete`：复制 `api`、`wiki` 后按相对路径和文件大小逐项校验，发现缺失、意外文件或大小不一致立即失败。随后重新执行 `sync-all`，以参考仓库同步结果完整回填 `raw/docs`。

## 验证

- 14 篇分类副本 SHA-256 与源文件一致。
- 同步脚本 `Sync-OasisDocs.ps1` 与双击运行入口 `双击运行本地同步.cmd` 语法正确、可用。
- 2026-08-28 实际同步：Wiki `314` 篇词条、`3705` 个镜像文件；API `6029` 个实体、`6034` 个镜像文件。
- 镜像校验：Wiki 源/目标均为 `3705` 个文件且 Markdown SHA-256 无差异；API 源/目标均为 `6034` 个文件且全量 SHA-256 无差异；Markdown 均无 UTF-8 BOM。

## 入口

- [项目实战案例索引](../../../../wiki/项目/项目实战案例索引.md)
- [双击运行本地同步.cmd](../../../../双击运行本地同步.cmd)
- [Sync-OasisDocs.ps1](../../../../tools/Sync-OasisDocs.ps1)
