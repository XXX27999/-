# IslandAuctionKing 选择完成后竞拍 UI 未打开

> 类型：项目证据
> 主题：PIE / AuctionTestUI / LuaInit / Button_384
> 适用范围：IslandAuctionKing，DebugID `dkck7sej3cle9p`
> 证据状态：项目实测；选择 RPC 已到达 DS，竞拍 UI 因 Construct 异常未打开；已改 Lua，待重新 PIE 验证
> 来源：`D:\WeGameApps\rail_apps\OasisEraEditor(2001776)\ShadowTrackerExtra\Saved\Logs\IslandAuctionKing\Clientlog\LuaLog\2026.09.04-11.11.49_client__dkck7sej3cle9p_1.log`、同会话 DS FullLog
> 更新时间：2026-09-04
> 关联主题：角色道具选择未跳转竞拍日志分析, PIE调试与热更新边界
> 排除范围：本轮不是 KHDRPC forbidden；不是文件名校验失败
> 官方依据：知识库未覆盖 Button_384 编辑器生成绑定；RPC 成功部分对照 `raw/docs/wiki/进阶内容/203_网络同步系统介绍.md`

## 结论

选择流程已经走通。客户端确认战术勘察员 + 初级泛用仪器组后，DS 收到 `ServerRPC_SelectAuctionPropGroup` / `ServerRPC_SelectAuctionCharacter`，`SelectionReady=true`。选择 UI 已关闭。竞拍界面没出现，是因为 `AuctionTestUI:LuaInit` 仍绑定已删除的 `Button_384`，Construct 在第一条业务日志前就 LuaPCallFailed。

## 关键证据

1. 客户端 `11.14.04`：`ConfirmSelection` 出口 `success=true pending=true`，随后 `HandleOperationResult` 对 `SelectPropGroup` 和 `SelectCharacter` 都成功。
2. `ApplyPrivateState` 关键数据：`PropSelectionConfirmed=true CharacterSelectionConfirmed=true SelectionReady=true`，并关闭选择 UI。
3. 立即调用 `AuctionTestUIService+CreateAndShow`，出口 `success=false auctionResult=WidgetInitializeFailed`。
4. Lua 异常：`CallLuaUFunction Failed: UFuncSig=[Construct]`，`AuctionTestUI.lua:406: attempt to index a nil value (field 'Button_384')`。
5. DS `11.14.02`：`KHDRPC_Call` 入口 `ServerRPC_SelectAuctionPropGroup` / `ServerRPC_SelectAuctionCharacter`，出口 `result=true`。这与 2026-08-28 的客户端 forbidden 不是同一故障。

## 修改

已改 `Script/Blueprint/Prefabs/UI/AuctionTestUI.lua`：`LuaInit` 在 `Button_384` 为空时跳过绑定；`Construct` 先打入口日志并用 pcall 保护 `LuaInit`。写前备份：`D:\知识库\和平精英绿洲起源\备份\IslandAuctionKing\20260904_AuctionTestUI_Button384`。
