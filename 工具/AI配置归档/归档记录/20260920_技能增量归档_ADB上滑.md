# 2026-09-20 技能增量归档（ADB 上滑）

> 类型：知识库工程资产 / 归档记录
> 主题：`oasis-ocr-sampler-calibrate` 技能增量入档
> 适用范围：本页仅登记 2026-09-20 新增入档的资产；全量快照见 `20260916_技能与MCP全量归档.md`
> 证据状态：项目实测（本机归档操作 + 逐文件 SHA256）
> 来源：2026-09-20 会话。用户要求修复 OasisOcrSampler「上滑失败」，过程中更新了该技能，按 [存放约定_新增技能插件MCP.md](../存放约定_新增技能插件MCP.md)「先归档、后部署」补档
> 更新时间：2026-09-20
> 关联主题：`../000_归档索引.md`、`../../../raw/知识/通用/工具与流程/绿洲启元地图数据OCR采样工具.md`
> 排除范围：客户端缓存目录（`plugins\cache`、`*.db`）；UGC 工程运行文件
> 官方依据：官方未覆盖；本条为仓库工程约定

---

## 背景

`oasis-ocr-sampler-calibrate` 在 2026-09-20 会话中新建并同日更新（新增「ADB 上滑通路」章节、`INJECT_EVENTS` 铁律与排查顺序、PowerShell 环境坑），但**不在 2026-09-16 全量快照内**。按「先归档、后部署」补齐归档层，使归档层与 WorkBuddy 生效层一致。

## 登记项

### AICFG-20260920-001 oasis-ocr-sampler-calibrate

- 资产类型：技能
- 归档绝对路径：`D:\知识库\和平精英绿洲起源\工具\AI配置归档\skills\workbuddy\oasis-ocr-sampler-calibrate\SKILL.md`
- 来源绝对路径：`C:\Users\Administrator\.workbuddy\skills\oasis-ocr-sampler-calibrate\SKILL.md`（本会话新建 + 同日更新）
- 部署目标绝对路径：`C:\Users\Administrator\.workbuddy\skills\oasis-ocr-sampler-calibrate\`
- 关联实体：技能名 `oasis-ocr-sampler-calibrate`；工具 `OasisOcrSampler.py`、`test_pipeline.py`、`test_regress.py`；函数 `diagnose_adb_error` / `Adb.screen_size` / `Adb.swipe_up`；常量 `INT_ROW_COUNT` / `INT_ROW_STEP` / `INT_COL_STEP` / `GEOM_VERSION`；报错实体 `INJECT_EVENTS`、`InputShellCommand.sendSwipe`；触发词「识别读不准」「读成 36」「一页几行几列」「只能框选一列」「上滑失败」「INJECT_EVENTS」
- 用途：标定/修正绿洲启元地图数据 OCR 采样器（大厅列表页 2 行 × 3 列识别）与 ADB 采样通路（含上滑被 ROM 拦的定案流程），并用真实截图 / 真机回归验证
- 调用入口：技能由 WorkBuddy 自动按触发词加载；工具本体执行入口
  `D:\知识库\和平精英绿洲起源\工具\OasisOcrSampler\.venv\Scripts\python.exe OasisOcrSampler.py`
  两个验证入口（改动后必跑）
  `D:\知识库\和平精英绿洲起源\工具\OasisOcrSampler\.venv\Scripts\python.exe test_pipeline.py`
  `D:\知识库\和平精英绿洲起源\工具\OasisOcrSampler\.venv\Scripts\python.exe test_regress.py`
- 输入 / 输出：输入=真机截图或拍照导入 + 手拖字段框；输出=`_selftest.txt` / `_regress.txt` / `crops\regress_boxes.png` / `out\*.csv|json`；技能本身无返回值
- 依赖：Python 3.12.10（工具专属 `.venv`，含 tkinter 8.6 + Pillow 12.3.0 + rapidocr-onnxruntime 1.4.4）；`adb.exe`（`D:\知识库\和平精英绿洲起源\工具\platform-tools\adb.exe`，未加 PATH）；真机需开「USB 调试（安全设置）」才能用自动上滑
- 版本：未标注（SKILL.md frontmatter 无 `metadata.version`）
- 证据状态：项目实测（2026-09-20 自检 A/A2/B/C/D/E/F 全通过、回归 26/30、真机端到端「上滑分类 / 截屏 / 黑屏成因」三项均符合预期）
- 生命周期：可复用；清理条件：工具 `OasisOcrSampler` 停用或技能被合并
- 更新时间：2026-09-20

## SHA256

```
93669B1C1439386C9794458B4D324DBA1C3DF618A12A5AB00C3FCBEB34770499  skills\workbuddy\oasis-ocr-sampler-calibrate\SKILL.md  17728
```

> 本技能在同一天被更新三次，归档已同步到**最终版**（17,728 B）。首次入档 15,007 B / `FB12DB…F96D95`；第二次补入「真机截屏通路与黑屏成因判定（`explain_blank` / `screen_awake`）」17,687 B / `9D856A…B8B422`；第三次把禁令改写为**锚定行为本身、无条件**的表述（原「为此上 root…」属动机条件式禁令，会把禁令挂到自证目的上、留下开脱钩子）。**以本节哈希为准。**

（已追加至 `校验清单_SHA256.txt`。本次增量共使清单条数 **3017 → 3019**：+1 本技能、+1 本记录文件；同时刷新了 `000_归档索引.md` 自身条目的哈希。）

## 顺带发现的归档问题（本轮实测）

`校验清单_SHA256.txt` 只覆盖 **3019** 个文件，而归档目录实际有 **3853** 个：**834 个未纳入校验**，其中 833 个位于 `skills\` 下 4 个「非分层目录」（`sqSkill` 787 / `自动读wiki+API` 36 / `美术技能` 7 / `ugc-log-bug-analyzer` 3），另 1 个是清单自身。经验证：**已纳入校验的 3017 条全部一致、0 缺失、0 漂移**（本轮仅 `000_归档索引.md` 因被改写而变更，已同步刷新）。详见 `../000_归档索引.md` 第三节与第十节第 4、5 条 —— **不要据此认为「校验清单全绿」等于「归档完整」**。

## 同步动作清单

- [x] 技能包写入归档层 `skills\workbuddy\oasis-ocr-sampler-calibrate\SKILL.md`
- [x] SHA256 追加至 `校验清单_SHA256.txt`（最终版 17,728 B；同日三次更新后已同步刷新）
- [x] 本页登记项
- [x] `000_归档索引.md` 第四节 4.1 表格行 + 4.4 来源分层计数（19 → 20）+ 第二节目录结构计数（18 → 19）
- [x] 归档内无 `__pycache__` / `*.pyc` / 临时文件（该技能包仅 1 个 `SKILL.md`）
- [ ] **未同步至 Codex 侧（`~/.codex/skills`）与 cc-switch 共享源** —— 本技能当前仅存在于 WorkBuddy 层；是否需要跨端一致未确认，见 `../000_归档索引.md` 第十节第 5 条

## 相关页面

- [000_归档索引.md](../000_归档索引.md)
- [存放约定_新增技能插件MCP.md](../存放约定_新增技能插件MCP.md)
- [20260916_技能与MCP全量归档.md](./20260916_技能与MCP全量归档.md)
- [绿洲启元地图数据OCR采样工具](../../../raw/知识/通用/工具与流程/绿洲启元地图数据OCR采样工具.md)
- [2026-09-20_ADB输入注入权限与上滑自检](../../../raw/知识/通用/来源记录/2026-09-20_ADB输入注入权限与上滑自检.md)
