# IslandAuctionKing 项目资料索引

> 项目名（合法化后）：`IslandAuctionKing`（中文别名：海岛竞拍王）
> 建立日期：2026-08-26
> 性质：**地图项目专属资料**，只做局部/按需索引，不进入 `wiki/000_索引.md` 的全局条目列表
> 全局侧入口：[wiki/项目/000_项目资料总览.md](../../wiki/项目/000_项目资料总览.md)
> 规则页：[wiki/概念/知识库分层与索引隔离.md](../../wiki/概念/知识库分层与索引隔离.md)

---

## 一、目录约定

| 子目录 | 收什么 |
| --- | --- |
| `开发记录/` | 本项目的功能实现、问题排查、修复记录 |
| `数据参数/` | 玩法数值、时长、容量、概率、坐标等具体参数快照 |
| `配置表/` | DataTable 行名/类型/值导出，配置表结构快照 |
| `蓝图与UI/` | 蓝图结构、UI 控件树、画刷基线等 MCP 回读产物 |
| `资产清单/` | 贴图、模型、音频等资产命名与导入属性清单 |
| `日志证据/` | PIE 日志片段、DS/客户端日志摘录 |
| `_JsonOutput/` | **工具产物**：`.uasset` 的 UAssetGUI JSON 只读镜像（2026-09-16 由工程内迁入，285 文件 / 370,893,592 B，284 JSON ↔ 工程 284 个 `.uasset`），只用于只读核查，禁止编辑 |

写入规则：只放本项目专属内容。任何可复用到其他项目的通用结论必须提炼后写进 `wiki/概念/`，本目录只保留项目侧证据。调试备份不写入本目录，统一放 `D:\知识库\和平精英绿洲起源\备份\IslandAuctionKing\`，清单见 [迁移清单](../../备份/IslandAuctionKing/000_迁移清单.md)。
非运行辅助文件（网页预览、Python 工具、素材暂存、Agent 工作记忆、转换工具与 JSON 镜像）登记在 [开发记录/000_辅助文件索引.md](./开发记录/000_辅助文件索引.md)，正文存放在 `D:\知识库\和平精英绿洲起源\备份\IslandAuctionKing\20260916_工程外置资料迁移\`；工程内不再保留这些文件。

---

## 二、本项目相关的既有原始资料（位于只读 `raw/docs/ai`，未搬迁）

| 文件 | 主题 | 项目标注依据 |
| --- | --- | --- |
| `raw/docs/ai/20260824_图鉴UI素材批量导入与视觉优化.md` | 图鉴 UI 贴图批量导入、配置表化、视觉优化 | 正文首部标注「项目：IslandAuctionKing (海岛竞拍王)」，正文含 19 处 `/IslandAuctionKing/` 资产路径 |

搬迁说明：`raw/docs/**` 是入库时的只读原始资料，且已被 23 个 wiki 页面按路径引用，因此**不做物理移动**，改为在本页登记归属。2026-08-26 之后新增的本项目资料一律直接落到本目录。

---

## 三、已提炼到全局的通用结论

以下结论已从本项目资料中提炼为通用知识，查通用做法请直接看概念页，不要回读项目资料：

- [资源导入与路径规范](../../wiki/概念/资源导入与路径规范.md)：UI 贴图 `LODGroup=16`、`MipGenSettings=13`、`CompressionSettings=0`、`SRGB=True`
- [配置表驱动开发](../../wiki/概念/配置表驱动开发.md)：贴图路径与 UI 尺寸不硬编码，全部登记为配置项
- [图鉴 UI 高保真还原](../../wiki/概念/图鉴UI高保真还原.md)
- [MCP UI 编辑与高保真还原知识库](../../wiki/概念/MCP-UI编辑与高保真还原知识库.md)
- [绿洲蓝图变量与玩家数据存储](../../wiki/概念/绿洲蓝图变量与玩家数据存储.md)
- [绿洲 UMG 文本自动包裹与换行](../../wiki/概念/绿洲UMG文本自动包裹与换行.md)
- [绿洲 UMG 设计态缩放裁剪与白块排错](../../wiki/概念/绿洲UMG设计态缩放裁剪与白块排错.md)

## 四、本项目证据索引

- [2026-09-24_道具组集齐单件解锁方案B](./开发记录/2026-09-24_道具组集齐单件解锁方案B.md) - 用户明确选择方案 B「集齐组内所有单件即可选择该组」。**语义定案**：组库存优先消耗；仅当 `PropGroups[组名]` 为 0 且 `OwnedProps` 组内单件全齐时，作为永久解锁放行，开局激活不扣组库存、不消耗单件；单件不全齐仍拒绝 `PropGroupUnavailable`。**代码**：`CharacterPropTestGrantService.ResolvePropGroupAccess` 新增第三入参 `group` 与第四返回值 `unlockedByOwnedProps`，按 `group.Items` 统计 `ownedPropCount/requiredPropCount`；`AuctionPlayerService:SelectPropGroup` 与 `ActivateSelectedPropGroup` 传入已解析 `group`，激活新增 `OwnedPropsUnlock` 分支（不改 `PropGroups`/`OwnedProps`，存档结构不变）。**验证**：luaparser 2/2 通过、`git diff --check` 返回 0、`lupa` 桩测试 20/20 通过（0库存4/4放行不扣、3/4拒绝、2库存激活扣到1）、工程内临时文件扫描 0。**未重新 PIE**。备份 `备份\IslandAuctionKing\20260924_集齐单件解锁道具组\`

- [2026-09-24_道具组确认被服务端拒绝导致开始匹配重开选择UI](./日志证据/2026-09-24_道具组确认被服务端拒绝导致开始匹配重开选择UI.md) - 用户反馈「选择完道具点击开始匹配仍跳出开始选择道具 UI，不会开始匹配」。**根因**：客户端 `GetGroupOwnership` 用 `OwnedProps` 组内 4/4 全齐判 `ownership=owned`，`Confirm` 发完 RPC 立即乐观隐藏 UI 并返回 `success=true`；服务端 `ResolvePropGroupAccess` 只认 `PropGroups[groupName] > 0` 或测试授权，本局 `inventoryCount=0 testGrant=false`，返回 `PropGroupUnavailable`，`PropSelectionConfirmed` 始终为 `false`。**双端证据（DebugID `_dkck7psp70i2e4`）**：客户端 `15:04:25` 点击开始匹配 → `尚未确认道具组` → `CreateAndShow`；`15:04:29.501` `Confirm requestOK=true`；`15:04:29.573` 收到 `Action=SelectPropGroup Success=false Message=PropGroupUnavailable`；DS `15:04:29.482` `inventoryCount=0 testGrant=false allowed=false` → `15:04:29.498 finalResult=PropGroupUnavailable`；`15:04:37` 再点匹配仍重开道具 UI。**结论**：不是开始匹配按钮故障，也不是二次匹配取消，而是两端道具组拥有态口径分裂 + 客户端乐观关闭。**修复方向**：方案 A 对齐客户端与服务端，仅服务端确认后关闭并置 `PropSelectionConfirmed`；方案 B 若改为「集齐单件即可用组」，必须同步改服务端权限与扣库存语义。**未改 Lua/蓝图/DataTable**

- [2026-09-24_竞拍金币被存档拉取失败覆盖与防覆盖守卫](./开发记录/2026-09-24_竞拍金币被存档拉取失败覆盖与防覆盖守卫.md) - 用户反馈「竞拍获取与扣除的金币不会影响玩家金币数据」。**根因**：PIE/DS 启动早期分块存档拉取失败时，`GetPlayerArchiveData` 返回空表，`NormalizeArchiveData` 以默认 `InitialGold=30000` 初始化，随后 `SavePlayer` 把 30000 覆盖真实存档。同日对照：失败局 `2026.09.24-14.19.43` DS 日志出现 `Failed to read chunk data` 三次后 `Max retry count reached`、`Gold=30000`；成功局 `2026.09.24-14.21.19` 读到 `Gold=29500`，落盘 `Saved\ArchiveData\IslandAuctionKing\10001.json` 也是 `29500`。**修复**：`AuctionPlayerService.lua` 新增 `HasGameArchive` 与 `GetArchiveByteSize`，用官方 `GetPlayerDataSize` 区分“无存档 0”与“有存档但本次读空”；后者及大小未知均拒绝进入本局（`ArchiveReadFailedWithServerData` / `ArchiveStateUnknown`）；`SavePlayer` 增加 `ArchiveReadValid` / `ArchiveMayCreate` 防线，不可信时返回 `ArchiveNotLoaded`，并在结算链路补 `goldBefore` / `delta` / `archiveGoldBefore` / `runtimeGold` 日志。**验证**：LuaCheck `51/51` 通过、`git diff --check` 通过、工程内 `$null` / 临时文件扫描 0；最终守卫版本尚未 PIE 复测。备份 `备份\IslandAuctionKing\20260924_竞拍金币存档修复\`。**可尝试热更新；建议重新 PIE**

- [2026-09-24_装扮主题解锁状态存档与忽略表UnlockState](./开发记录/2026-09-24_装扮主题解锁状态存档与忽略表UnlockState.md) - 装扮主题解锁状态改为玩家跨局存档数据，并停止依赖主题表 `UnlockState` 列。**蓝图**：`/IslandAuctionKing/Asset/Blueprint/UGCPlayerController` 新增 `AuctionDressThemeUnlockState`（`FString`，flags `65541`，plan_id `plan_16791119_52c51972`），MCP 回读通过；全资产与全历史扫描确认控制器历史上没有其它 `Dress*` 存储变量可删，`AuctionUITexturePaths` 因仍被贴图替换链路使用而保留。**存档链路**：`AuctionPlayerService.lua` 的 `CreateDefaultArchiveData`/`CreateBot` 增加 `DressThemeUnlockState = {}`，`NormalizeArchiveData` 规范化为 `{[themeId]=true}`，`LoadOrReconnectPlayer` 读档，`SyncControllerOwnership`/`BuildPrivateState`/`SavePlayer` 写回；`CF_FWDRPC.lua` 缓存私有状态并转逗号串写控制器，变化时懒加载 `AuctionDressUIService.RefreshUnlockState`。**忽略 `UnlockState`**：`AuctionDressUIService.lua` 的 `LoadStyleThemes` 改为 `Unlocked = IsThemeUnlocked(widget, themeId)`，默认主题始终解锁，其余只认控制器镜像的玩家存档数据，表列不再参与判定。**验证**：luacheck `ReturnCode: 0`；PIE DebugID `_dkck7psp70h6ft` 服务端 `restoredThemeCount=0 archiveVersion=3`、客户端 `AuctionDressThemeUnlockState=`；落盘 `Saved\ArchiveData\IslandAuctionKing\10001.json` 含 `"DressThemeUnlockState": []`、`"Version": 3`；工程内 `$null` / 临时文件扫描为 0。**未验证**：非空解锁集合的跨局往返；当前无解锁业务入口。备份 `备份\IslandAuctionKing\20260924_装扮主题解锁存档\`。**必须重新 PIE**

- [2026-09-24_null文件再次出现与并发命令根因](./日志证据/2026-09-24_null文件再次出现与并发命令根因.md) - 用户复问「为什么项目中还是会出现 `$null` 阻碍 PIE」。**复核结果**：工程根 `$null` 已由并发会话删除，但 `Saved\UGCLinuxDebug\IslandAuctionKing\$null` 仍在；本次删除后对 `ShadowTrackerExtra` 复扫为 0。**根因**：并发 Codex 会话 `01a0d216-d64a-7db3-ac18-ed5e2f9512f4` 已加载禁止 `$null` 规则，仍在 14:26:17 以 `shell:"bash"` 发出 `rg ... 2>$null | Select-Object ...`；该次输出为 `'Select-Object' is not recognized as an internal or external command`、进程码 255，同秒工程根生成 0 字节 `$null`。14:26:31 改用 `shell:"powershell"` 重试正常。14:26:49 PIE 递归复制工程到上传目录，副本晚于源文件 32 秒，证明 PIE 不是源头。**结论**：规则只约束后续命令，不会自动改写已发命令或清理落盘文件；清理必须覆盖工程根和上传副本并复扫 0。**未改 Lua 或蓝图**

- [2026-09-24_轮次详情角色技能底板可见性修复](./开发记录/2026-09-24_轮次详情角色技能底板可见性修复.md) - 轮次详情弹窗「角色技能」标题框下方技能说明底板不可见。**根因**：`AuctionTestUI` 控件 `Image_3`（与 `RoundDetailSecurityText` 同坐标 42,481 204x88，ZOrder -10）设计态 `Visibility=Hidden(2)`；`AuctionUITextureReplacementService` 的 `IMAGE_BINDINGS` 虽已含 `Image_3 → RoundDetailInfoFrame` 且运行时 `brushOK=true tintOK=true`，但 `ApplyImageTexture` 只调 `SetBrushFromTexture`+`SetColorAndOpacity` 不改可见性，`AuctionRoundHistoryUIService.OpenRoundDetail` 也只设文本可见性 → 画刷绑上仍不渲染。**修复**：MCP `widget_set_property(bp,"Image_3","Visibility","HitTestInvisible")` + compile + save（plan_id `plan_16782840_20e47c7f`），回读 `Visibility: HitTestInvisible`，`slot_ZOrder=-10` 与 `Image_0/1/2` 未变。**同时修复 PIE 调试报错**：上传目录 `Saved/UGCLinuxDebug/IslandAuctionKing\$null` 残留 0 字节 `$` 开头文件 → `CheckUploadFileNameFailed Result 10001` 与连锁 `CheckLua Failed (Parent=CancelDebug, luacheck ReturnCode 0)`；删除工程根与上传目录两处 `$null` 后恢复。**验证**：双客户端 PIE（DebugID `_dkck7psp70b7yl`，mobile）`CheckUploadFileName Result 0` + `lua file validation passed` + `DS Ready`；运行时 `doluastring` 回读 `Image_3 vis=3 (HitTestInvisible) opacity=1.0 brush=true`，与 `RoundDetailSecurityText vis=3` 一致（修复前为 `vis=2 Hidden`）。备份 `备份\IslandAuctionKing\20260924_RoundDetailSkillFrame\`。**必须重新 PIE**

- [2026-09-24_图鉴文字紫色_FSlateColor包装修复](./开发记录/2026-09-24_图鉴文字紫色_FSlateColor包装修复.md) - 图鉴标题、进度、详情、条目名称与收集状态整体发紫。**根因**：`CollectibleCodexUIService.lua` 10 处 `TextBlock:SetColorAndOpacity` 直接传 `{R,G,B,A}`，而官方 `UTextBlock.SetColorAndOpacity` 入参为 `FSlateColor`；slua 静默解析失败后回退默认前景色，两个图鉴 Widget 的默认 `ForegroundColor` 恰为品红 `(1,0,1,1)`。**修复**：新增 `ApplyCodexTextColor`，统一用 `{SpecifiedColor = linearColor}` 包装并保留 `FLinearColor` 兜底；`ItemImage` 的图片颜色不改。**验证**：luaparser 通过、UTF-8 无 BOM、`git diff --check` 通过、MCP 只读回读确认 CDO 默认品红、工程内无临时文件；**未 PIE**。**可热更新**

- [2026-09-23_服务端决策人机](./开发记录/2026-09-23_服务端决策人机.md) - 官方假人是 Pawn/AIController，不接入拍卖 RPC 与存档。本轮落地服务端决策席位：`AuctionBotService` 补到 `RequiredPlayers`，选角、用道具、按情报或先验估值出价并锁定；`IsBot` 不写存档。`BotConfigTable` 16 行，MCP 回读 18 行，含 `Bot.ConservativeMargin=0.08`、`Bot.AggressiveMargin=-0.08`。人机无控制器，角色和道具组权限改为认 `IsBot`；临时道具组不写存档。luaparser 通过，**未启动 PIE**。运行链路已在 [2026-09-24_20秒未满补人机匹配](./开发记录/2026-09-24_20秒未满补人机匹配.md) 的 PIE 实测中闭环

- [2026-09-24_20秒未满补人机匹配](./开发记录/2026-09-24_20秒未满补人机匹配.md) - 开始匹配后 20 秒内匹不满 4 人时，先聚集当前真人再按缺额补人机到 4 人开局。**官方依据**：`20062_阶段匹配.md` 阶段时长 5/5/2/2 后续各 1s，前 10 段累计正好 20s，故把「1 人成局」放到第 11 段。`[MultiModeSetting_1]`（ModeID=1002）`bIsAllowStageMatching=1` + `TeamSettings` 由 1 段改为 **11 段**（前 10 段 `4x1`，第 11 段 `1x1`）。配置表：`Gameplay.Match.RequiredPlayers=4`、`Gameplay.Match.BotFillDelay` 3→**2**；`BotConfigTable` 18 行。窗口语义：无真人不开窗（防人机自行开赛）；真人未满员开 2s 聚集窗；到期按缺额补位；满员时真人挤掉最后一名人机；最后真人离场拒绝补满。**本轮修 2 个真 bug**：① `AuctionBotService` 窗口状态机 `FillRemainSeconds<=0` 死锁（delay=0 永不补位），改 `FillWindowExpired` 标记；② `AuctionPlayerService:RemovePlayer` 人机 `UID=nil` 时 `PlayerKeyByUID[nil]` 抛 `table index is nil`，改 `tonumber(player.UID)` 判定。**PIE 单客户端实测通过（DebugID `_dkck7psp7061h6`）**：10:30:50 真人登录 → `StartFillWindow remain=2 currentCount=1`；10:30:51 窗口到期 → `FillSeatsAfterWindow missing=3` → 3 人机 `-910001..3` → `participantCount=4` → `StartPreparing` → `Phase=Bidding`，全程无 Lua 报错。备份 `备份\IslandAuctionKing\20260923_MatchFillWindow\`。**必须重新 PIE**

- [2026-09-24_20秒阶段匹配与实际2秒人机补位](./日志证据/2026-09-24_20秒阶段匹配与实际2秒人机补位.md) - 用户复报「开始匹配还没 20 秒，也没匹配到 3 个真人就开始游戏由人机补位」。**定性**：20 秒是官方阶段匹配前 10 段累计时长（`5+5+2+2+1*6`），不是真人进入新 DS 后的补位等待；项目补位走 `Gameplay.Match.BotFillDelay=2` 秒。**双端日志**：客户端 `14:24:44.452` 收到 `RequestMatch second=true`、`14:24:45.260~14:24:54.265` 匹配计时 `00:00~00:09`；新 DS（DebugID `_dkck7psp70fud7`）`14:24:54.535` 读到 `BotFillDelay=2`，`14:25:30.618` 真人 `10001` 登录且 `currentCount=1 humanCount=1 requiredPlayers=4` 开 2 秒窗口，`14:25:32.135` 窗口到期 `missing=3`，补人机 `-910001..3`，`14:25:32.139` 即 `StartPreparing reason=BotFillSeats`。**官方依据**：`20062_阶段匹配.md` 与 `376_二次匹配功能.md`（Debug 二次匹配任意玩家可发起、必定成功、不可取消）。**结论**：PIE 二次匹配不等待真机阶段匹配，当前 1 真人 + 2 秒窗口 + 3 人机开局是配置必然结果，不是异常。**未改代码或配置**

- [2026-09-24_真人优先匹配与开局宽限修复](./开发记录/2026-09-24_真人优先匹配与开局宽限修复.md) - 用户追问「真机一直匹不到 3 个人会等到 20 秒才补人机吗，必须优先真人匹配」。**真机判定**：官方前 10 段 4 人成局累计 20 秒，第 20 秒起放行；但新 DS 内项目 `BotFillDelay=2` 秒就补人机，慢加载真人几乎没机会，属真实缺陷。**修复前 2 个缺陷**：① 2 秒窗口不随新真人延长；② `FillSeats` 补满即开局，`MakeRoomForHuman` 要求 `Phase==Waiting`，慢加载真人被挡在 `Preparing` 外。**改动**：`GameplayConfigTable` 21→**23 行**新增 `Gameplay.Match.HumanJoinExtendsWindow=true`、`Gameplay.Match.BotStartGrace=8`（plan_id `plan_16796102_4f9baa5d`，MCP 回读四行值一致）；`AuctionConfig.lua` 新增 `GetHumanJoinExtendsWindow()` / `GetBotStartGraceSeconds()`；`AuctionBotService.lua` 新增 `StartGraceDeadline` / `IsStartGraceActive` / `RefreshStartGrace`，真人登录重置 2 秒窗口、补满后进入 8 秒开局宽限、宽限内真人挤掉人机后重新计时、宽限到点由 `Tick` 统一放行、缺额兜底补位并短间隔重试；`AuctionGameService.lua` 在 `TryStartAfterSelections` 加 `BotFillGracePending` 门禁，`OnPlayerLogin` 按真人席位分支，`OnPlayerExit` 补位跳过宽限。**验证**：luaparser 3/3 通过、MCP 回读通过、UTF-8 无 BOM、工程内临时文件 0 命中；**未重新 PIE**，配置表与初始化改动必须重新 PIE。备份 `备份\IslandAuctionKing\20260924_HumanPriorityMatch\`

- [2026-09-24_人机名字随机与拟人出价道具](./开发记录/2026-09-24_人机名字随机与拟人出价道具.md) - 人机随机名字、随机角色/道具组、与玩家一致的道具使用、拟人化出价。**脚本**（`AuctionBotService.lua`）新增 4 个模块级纯函数：`ParseStringList`、`GetBidStepForMagnitude`（>=7 位 10000 / 6 位 1000 / 5 位 100 / 4 位 100 / 其余 10）、`HumanizeBid`（按量级取整保证尾数为 0，`Bot.LuckyBidChance` 概率改用 555/666/888 重复数字，仅当重复位为 5/6/8、相对偏离 ≤ `Bot.LuckyBidMaxDeviation` 且落在 `[minimumBid, ceiling]`）、`BuildBotDisplayName`（排除本局已占用名后随机取名，池子用尽回退 `Bot.DisplayNamePrefix..席位号`）。`SpawnOne` 改用随机显示名（复用同源线性同余序列）；`ChooseLoadout` 角色与道具组由按席位轮换改**随机**；`TryUseProp` 随机判定是否使用 + 只从 `IsPropUsed` 未标记道具中**随机抽一件**，仍走真人同一条 `PropUseService.Execute → MarkPropUsed → MarkPropUsed(子轮) → 历史` 链路。`DecideBid` 新增 `rawDesired` 中间值并经 `HumanizeBid` 收敛。`ResolveSettlementPlayerIdentity` 优先取 `player.DisplayName`；`AuctionTestUIService` 席位副标题不再对人机显示「人机席位」，避免暴露身份。**配置表** `BotConfigTable` 18→**21 行**（`Bot.NamePool` 18 个名字 / `Bot.LuckyBidChance=0.15` / `Bot.LuckyBidMaxDeviation=0.2`，plan_id `plan_16780763_6af3eeac`）。**验证**：luaparser 5/5 通过；lupa 直接加载工程内函数实测 `700→666 / 600→555 / 900→888`、`10000→10000 / 15400→15400 / 600→600 / 1540000→1540000`、越界与上限夹取、名字池 5/5 不重复 + 池尽回退。**PIE 单客户端实测通过（DebugID `_dkck7psp708u3b`）**：`DisplayName=小唐/小北/阿杰`、角色 `数据分析师/装备鉴定师`、道具组 `异象仪器组/进阶泛用仪器组`、`TryUseProp` 随机跳过与随机抽件并存且跨子轮不复用、`rawDesired=17625→desired=17600` 等报价收尾为整百，全程无 Lua 报错。备份 `备份\IslandAuctionKing\20260924_BotLoadoutNameBid\`。**必须重新 PIE**

- [2026-09-24_人机道具概率按轮递减与轮次详情玩家名](./开发记录/2026-09-24_人机道具概率按轮递减与轮次详情玩家名.md) - ①人机道具使用概率改为**按竞价子轮递减**：`AuctionBotService.lua` 新增模块级 `GetPropUseChance(bidRound)`，读 `Bot.PropUseChanceByRound`（英文逗号分隔序列，缺项回退 `Bot.PropUseChance`，结果 clamp `[0,1]`），`TryUseProp` 改用它并补日志 `BidRound=<N> chance=<X>`。`BotConfigTable` 21→**22 行**，新增 `Bot.PropUseChanceByRound = "0.9,0.7,0.5,0.3,0.15,0.1"`（string，plan_id `plan_16781828_23570973`），MCP 回读四列值一致。②轮次详情玩家名不同步：根因 `OpenRoundDetail` 用 `slotData.PlayerKey`（如 `-910001`）渲染标题与玩家名，而计分板用 `PublicScores[].DisplayName`；新增 `FindPublicDisplayName(publicState, playerKey)` 从 `AuctionPublicState.PublicScores` 取展示名（未命中回退 `玩家 <PlayerKey>`、空 key 回退 `未知玩家`），同时用于 `RoundDetailTitleText` 与 `RoundDetailPlayerText`，出口日志补 `PlayerKey/displayName`。**验证**：luaparser 2/2 通过、`git diff --check` 通过、lupa 单测 1~6 轮 = `0.9/0.7/0.5/0.3/0.15/0.1` 单调不增且缺项/clamp 正确、工程内无临时文件。**PIE 单客户端实测（DebugID `_dkck7psp709pxw`）**：DS 自然对局命中 `BidRound=1/3/4/5` 的对应 chance；探针用种子 `33`（首个 `NextUnit=0.9575`）逐轮触发随机门禁，得到完整六轮序列 `0.9/0.7/0.5/0.3/0.15/0.1` 且**未消耗道具**；客户端 `OpenRoundDetail` 实测 `-910002 displayName=阿斌`、`-910003 displayName=阿豪`、`10001 displayName=玩家 10001`（真人未命中回退），已无标题直拼 PlayerKey。**注意**：探针一度用种子 `2147483646` 触发真实道具消耗（`-910001` 用了 3 件），仅发生在 `Phase=Ended` 本局运行时且人机不写存档，无持久化副作用；探针已恢复 `BidRound`/`BotSeed`。备份 `备份\IslandAuctionKing\20260924_BotPropChanceRoundName\`。**必须重新 PIE**

- [2026-09-24_结算仓库藏品点击图鉴](./开发记录/2026-09-24_结算仓库藏品点击图鉴.md) - 结算界面右侧仓库已展示藏品点击后打开藏品图鉴并自动筛选。**脚本**：`AuctionSettlementUIService.lua` 新增 `BindCodexFilterContext`（以 `Phase.Ended` + 结算仓库/公共仓库索引补齐 `AuctionPlayerController`、`AuctionGameState`、`AuctionPublicState`、`AuctionPrivateState`、`AuctionItemByIndex`）、`OpenCodexForSettlementItem`（按 `Index`/`ID` 定位后复用 `AuctionCollectibleCodexFilterService.OpenForWarehouseItem`）；`ConfigureImageOnlyWidget` 增加 `allowClick`，结算藏品覆盖层的 `ItemButton` 改为透明 `Visible`，揭示动画层保持 `HitTestInvisible`；新增 `DestroySettlementOverlay` 统一解绑点击回调。**官方依据**：`UButton.OnClicked` 可点击；`Visible` 参与命中、`HitTestInvisible` 不参与。**验证**：luaparse 2/2 通过、`git diff --check` 通过、工程内无临时文件；**未 PIE**。备份 `备份\IslandAuctionKing\20260924_结算仓库藏品点击图鉴\`。**建议重新 PIE**

- [2026-09-18_单人PIE二次匹配停留在主界面](./日志证据/2026-09-18_单人PIE二次匹配停留在主界面.md) - 「点开始匹配 → 确实进了新 DS → 加载完仍停在主界面」的**根因**：`AuctionConfig.RequiredPlayers = 2`（`AuctionConfig.lua:21`，「双客户端 PIE 测试所需玩家数」）而单人 PIE 只有 `participantCount=1`，`AuctionGameService:CanStartWithCurrentHouse()`（`AuctionGameService.lua:341`）恒判 **「人数不足」** → `TryStartAfterSelections` 恒 `error=HouseRequirementBlocked` → 服务端 `Phase` 永远 `Waiting`；而**客户端对 `Waiting` 的设计就是「创建或复用主界面大厅」**（`AuctionClientPhaseUIService.ApplyPhaseUI`），故「加载完还是主界面」是当前实现的必然结果。链路双端日志坐实（DebugID `_dkck7pp6wwnk1n` 客户端 / `_dkck7pp6wwnk1o` 新 DS）：10:54:59 `RequestMatch` 回调 `result=true`（PIE 任意玩家发起必定成功）→ 10:55:07 新 DS `Phase=Waiting`、`DSEndTimeStamp=3600` → 10:55:45 客户端 `ResolveGameState 出口 gameState=nil` + `CachePending reason=PlayerLogin`（确实换局）→ 10:55:56 三次 `ApplyPhaseUI` 全走「等待阶段已创建或复用主界面大厅」→ DS 三次 `readyCount≤1 requiredPlayers=2 participantCount=1`→「人数不足」→ 再点匹配因 `modeID=1002` 判 **`AlreadyInAuctionSubMode`** 静默短路（`matchOK=false`）。**重要纠正**：`Test.SkipWaitingPhase=true` 只绕过**子模式限制**（`AuctionGameService.lua:378-382` 把 `inAuctionMode` 置真），**不能**绕过人数门槛。**结论**：需改用**双人 PIE**（`players_per_team=2`）才能进竞拍界面；这同时解释了历史多轮 `AuctionTestUI+Construct` 命中数恒为 0（单人从未真正开过局）。与 2026-09-10 的「OnRep 未触发 / StartBidRound 只 Refresh 不关大厅」不是同一问题，也与装扮主题画刷改动无关。**双人 PIE 实测已闭环（2026-09-20，DebugID `_dkck7pp6wwoo9p` / 新 DS `_dkck7pp6wwoo9q`）**：第二客户端登录后 `participantCount=2` → `IsCurrentAuctionSubMode 出口 inAuction=true`（`currentModeID=1002`）→ `CanStartWithCurrentHouse 出口 可以开局` → `StartPreparing 出口 PhaseEndTime=45.47…` → 11:04:39 `StartBidRound 出口 BidRound=1`、`Tick 出口 currentPhase=Bidding`；对照旧 DS（`currentModeID=0`、`inAuction=false`、`人数不足`）说明**必须等二次匹配切到 `modeID=1002` 的新 DS** 人数判定才通过。详见该页 §6

- [2026-09-18_装扮主题索引与素材目录迁移](./配置表/2026-09-18_装扮主题索引与素材目录迁移.md) - 装扮 UI 主题切换系统**含同日续跑的 PIE 运行验证与加载时序修复**。**时序缺陷（本轮关键发现）**：主界面/竞拍界面 `Initialize` 由 widget `Construct` 触发，早于 `UGCGameState:ReceiveBeginPlay` 的 `AuctionConfig.LoadDressConfigTable`，两轮 PIE 实测早 **129ms / 142ms**，导致 `GetUiSlotTargets` 槽位全部按「DressControlTable 值为空串」跳过、`themeAppliedCount=0`，而同一帧稍后装载日志为 `success=true`——**不报错，只表现为功能不生效**；修复范式（详[界面初始化早于配置表装载与惰性应用](../../raw/知识/通用/UI与交互/界面初始化早于配置表装载与惰性应用.md)）：`AuctionConfig` 出口写 `DressConfigLoaded`/`DressControlLoaded` 标记 → `AuctionDressThemeService.ApplyStyleToHostUIWhenReady` 在 `Initialize` 末尾 + 各 `Tick` 每帧调用，未就绪空转等待（`waitFrames % 60` 节流）、就绪后只应用一次，状态写 widget 字段（不持有界面引用）且主题变更自动重应用；另加模块内 `GetCurrentStyleSilently` + 默认主题缓存，保证每帧快速路径不读表不打日志。**PIE 复验通过（主界面）**：`_dkck7pp6wwnk1n`，客户端 `LoadDressConfigTable 出口 appliedCount=107 dressControlLoaded=true`（89+18），DS 同值；`ApplyStyleToHostUIWhenReady uiKey=Hub allOK=true appliedCount=1 totalCount=1`、`ApplySlot 出口 success=true slotKey=HubBackground`（`参考图` UImage 两参 `SetBrushFromTexture`），各命中数恰为 1（一次性语义生效、不刷屏），`Tick` 错误 0；窗口截图确认主界面整屏渲染正常。**竞拍界面运行时证据已补齐（2026-09-20 双人 PIE）**：`GetUiSlotTargets uiKey=Auction targetCount=7 skippedCount=5` → `ApplyStyle 出口 allOK=true appliedCount=7 totalCount=7`，7 槽位逐个 `ApplySlot 出口 success=true`（目标均为 `UBorder`，两参 `SetBrushFromTexture`），双端一致；同会话 DS 已推进到 `Phase=Bidding`（`StartBidRound 出口 BidRound=1`），详见 [2026-09-18_装扮主题双人PIE双界面验证](./日志证据/2026-09-18_装扮主题双人PIE双界面验证.md)；**结算界面**仍未取得运行时证据（到 `Bidding` 后客户端崩溃，崩溃为既有缺陷）。**并发/覆盖核查**：`AuctionHubUIService` 无任何 `SetBrush*`、`AuctionUITextureReplacementService.Refresh` 全工程无调用方、`AuctionTestUIService` 唯一 `SetBrushFromTexture`（1018 行）属仓库藏品图，故一次性应用不会被后续刷新冲掉。luaparser 5/5 OK、工程内 0 临时文件；git 分支 `dress-theme-20260918` 本地提交 `028ce94`（4 files / +205 −20），**推送仍未成功（401 认证，需用户提供 PAT 或 SSH 公钥）**

- [2026-09-18_装扮主题索引与素材目录迁移](./配置表/2026-09-18_装扮主题索引与素材目录迁移.md) - 装扮 UI 主题切换系统落地：结构定为**两层** `Asset/TuPian/Dress/<StyleTheme>/<SlotKey>`（三界面现有边界素材合起来即一个主题，故不做每材质一份拷贝）；**素材物理迁移 8 个 SlotKey 成功**（HubBackground/AuctionBackground/AuctionTopBar/AuctionBottomBar/AuctionIntel/AuctionWarehouse/BidBoard/PropUsePanel，源分别为 ZJMBackground/AuctionUI_Background/AuctionUI_TopBottomBar×2/AuctionUI_CenterPanel/AuctionUI_WarehousePanel/AuctionUI_BidKeypad/SkillPanel），MCP `load_object(Texture2D)` 8/8 通过；**`ue.duplicate_asset` 复制贴图只产出 546/554 字节空壳、不可用**，可行路径是 PowerShell `Copy-Item` + `ue.rename_asset('<包路径>.<源对象名>','<SlotKey>')`（修正包内导出名，rename 一批 ≤3）；`DressMaterialTable` **120 行** Value 由不存在的 `Asset/TuPian/ImageMaterial/aN/aN` 改指 `Asset/TuPian/Dress/DefaultGray/<SlotKey>`，回读仍 121 行 / 8 SlotKey × 15 索引未丢失；`DressConfigTable` 60→**89 行**（新增 29 行 `Dress.Theme.*` 主题索引 + 3 开关）；**未新建 `DressThemeTable`**（三种建表入口全部产出无对象的空包），主题索引改放 `DressConfigTable` 用前缀隔离，并删除失败残留表与副作用资产 `UGCTemplateRowStruct_DressThemeTable`；**发现 `AuctionConfig.DressConfigTablePath` 声明了但从未被任何加载器引用**，`Dress.*`/`UI.Dress.*` 一直走代码回退值，本轮补 `AuctionConfig.LoadDressConfigTable` 并在 `LoadFromTable` 调用；新增 `Script/Function/AuctionDressThemeService.lua`（341 行，按主题名定位文件夹 → 槽位名索引同名素材 → 加载替换画刷，缓存键 `styleId→slotKey`，缺失回退 DefaultGray）；`AuctionDressUIService` 接入 `ApplyStyleThemeToRegion` / `SelectStyleTheme` / `GetStyleTheme`；luaparser 3/3 OK、工程内 0 临时文件；**未启动 PIE**；主题切换按钮 UI 未新增（新 UI 须先出网页预览并经用户确认）；git 分支 `dress-theme-20260918` 本地提交 `51889fa`，**推送未成功（实测为 401 认证问题，非 TLS/代理）**

- [2026-09-18_装扮主题双人PIE双界面验证](./日志证据/2026-09-18_装扮主题双人PIE双界面验证.md) - **装扮主题运行时验证闭环（双人 PIE，DebugID `_dkck7pp6wwoo9p`）**：①**主界面** `uiKey=Hub targetCount=1 skippedCount=11` → `ApplySlot 出口 success=true slotKey=HubBackground`（`参考图` UImage 两参）；②**竞拍界面** `uiKey=Auction targetCount=7 skippedCount=5` → `ApplyStyle 出口 allOK=true appliedCount=7 totalCount=7`，7 槽位逐个成功（`AuctionBackground→Border HUDBackgroundPanel`、`AuctionIntel→RightDetailPanel`、`AuctionWarehouse→WarehousePanel`、`AuctionTopBar→TopBarPanel`、`AuctionBottomBar→FooterPanel`、`BidBoard→BidKeypadPanel`、`PropUsePanel→PropPanel`，**全为 UBorder、全走两参 `SetBrushFromTexture`**）；③**惰性应用范式在竞拍界面复现**：`ApplyStyleToHostUIWhenReady uiKey=Auction waitFrames=1`（11:04:46.358）→ `LoadDressConfigTable 出口 dressControlLoaded=true`（11:04:46.786）→ `出口 applied=true uiKey=Auction`（11:04:46.802）；④**双端逐项一致**（`_1`/`_2` 的 `appliedCount`/`totalCount`/`allOK`/贴图路径相同）；⑤**DS 同步推进到 `Phase=Bidding`**（`participantCount=2` → `CanStartWithCurrentHouse 出口 可以开局` → `StartPreparing` → `StartBidRound 出口 BidRound=1`）。**方法论（重要）**：**客户端日志 0 字节 ≠ 没有日志**——UE 崩溃时把该会话日志搬进 `Saved\Crashes\UE4CC-Windows-<ID>_0000\`，原路径被截为 0 B，本轮 21.7MB / 23.2MB 客户端全量日志即从此处取回；`MainWindowTitle` 出现 `Assertion Failed` 或 `Responding=False` 即客户端卡断言/已崩。**并附 `LastExtraMaterialInfo` 误判排除**：该字段是环境快照不是断言原因（两次断言分别附带游戏自带 `Icon_OB_Img_Crit` 与本项目装扮贴图，却同为一断言）。**排除范围**：不证明主题切换按钮（无 UI 入口）、不证明结算界面
- [2026-09-20_同名画刷主题切换机制](./配置表/2026-09-20_同名画刷主题切换机制.md) - 主界面「同名画刷」主题切换机制（与 SlotKey 槽位机制**并行的第二套**）：素材文件名 = **主界面控件现用素材原名**（`ZJMBackground` / `ZJM5`…），目录规则 `<Dress.SameName.ThemeRoot>/<BaseFolder><Suffix>`（`Hub` 基目录 `ZhuJieMian`；默认主题 `DefaultGray` **不加后缀** → 直接读 `Asset/TuPian/ZhuJieMian`，保证「默认态=什么都不换」；非默认主题 → `Asset/TuPian/ZhuJieMian_<ThemeId>`，如 `TestTopic` → `Asset/TuPian/ZhuJieMian_TestTopic`），完整路径 `Asset/TuPian/<Folder>/<Texture>`；映射方向「**素材名 → 控件名列表**」落在 `DressControlTable` 的 `Dress.SameName.Texture.Hub.<素材名>`，共 **11 素材 → 14 控件**（由 UE 侧递归遍历 `ZhuJieMian` 控件树实测导出：`ZJMBackground`→参考图、`ZJM1`→每日任务上方UI+帮助上方UI+保底金上方UI、`ZJM3`→玩家名称+服务器剩余时间、`ZJM5`→头像框、`ZJM6`→帮助、`ZJM7`→每日任务、`ZJM8`→金币、`ZJM9`→金币UI、`ZJM10`→顶部UI、`ZJM11`→保底金、`ZJM12`→Image_160；**17 个 TextBlock 无画刷、12 个 Button 四态 `ResourceObject` 全 `None`、`ZJM2`/`ZJM4` 未被任何控件使用**，故均不纳入）；生效范围仅 `UImage.Brush` / `UBorder.Background` 贴图（UImage **两参** / UBorder **单参** `SetBrushFromTexture`；读当前贴图用 `Brush.ResourceObject`），不改布局锚点/色调/按钮四态/文案，仅客户端展示层；配置：`DressConfigTable` 89→**101 行**（11 行 `Dress.SameName.*` + `Dress.Theme.StyleIds` 补 `TestTopic` + `Dress.Theme.Style.TestTopic.Name`）、`DressControlTable` 18→**29 行**；`AuctionDressThemeService.lua` 新增 `SameName*` / `ApplySameName*` 函数族（33844 B→66285 B），`AuctionHubUIService` 在 `Initialize` 末尾与 `Tick`（`AuctionHubInitialized` 守卫）接入惰性入口 `ApplySameNameThemeToHostUIWhenReady`，**排在 SlotKey 应用之后**（两套并行、互不影响）。**两轮 PIE**：第一轮 `_dkck7pp6wwstp8` 抓到 **2 个真缺陷**——① `Dress.SameName.PathTemplate` 初值 `Asset/TuPian/<Folder>/<Texture>` 与 `ThemeRoot` 叠加导致路径重复成 `Asset/TuPian/Asset/TuPian/ZhuJieMian/ZJM1`（11 素材全 `success=false`、`appliedCount=0`），改值为 `<Folder>/<Texture>`；② 惰性入口**先判机制开关再判表已装载**，装载前读 `Dress.SameName.Enabled` 报「中间节点不是table」并把 `ResolvedDebugTheme` 缓存为空串（功能静默不生效），判定次序改为「**界面非空 → 表已装载 → 机制已启用 → 快速路径 → 应用**」，且 `GetCurrentStyleSilentlyForSameName` 仅在 `IsDressControlLoaded()` 为真时读调试主题。第二轮 `_dkck7pp6wwsxum` **全绿**：`BuildSameNameFolder … folder=Asset/TuPian/ZhuJieMian`（默认主题不加后缀）、`BuildSameNamePath … path=Asset/TuPian/ZhuJieMian/ZJM1`（不再重复根目录）、11/11 `LoadSameNameTexture 出口 success=true`（`usedFallback=false`）、14 行 `ApplyTextureToTarget 分支 UImage两参形式成功`、`ApplySameNameStyle 出口 appliedCount=14 totalCount=14 textureCount=11`、`ApplySameNameThemeToHostUIWhenReady 出口 applied=true uiKey=Hub`，同帧 SlotKey 机制并行 `ApplyStyleToHostUI 出口 allOK=true appliedCount=1 totalCount=1`，惰性等待 `waitFrames=1`，装扮相关 error **0 条**。**方法论**：**客户端日志 0 B 三种成因**（PIE 运行中 0 B 属正常、进程退出才落盘 / 崩溃 0 B 且存在 `Saved\Crashes\UE4CC-Windows-<ID>_0000\` / 未启动 0 B 无进程），已补进 `ugc-log-bug-analyzer` 技能 `crash_forensics` 说明节。**排除范围**：结算与竞拍界面同名替换未做、主题切换按钮 UI 未新增（靠 `Dress.SameName.DebugThemeId` 调试开关验证目录接通，**验证完须置回空串**）、`Asset/TuPian/ZhuJieMian_TestTopic` 目录本轮未实际创建
- [2026-09-20_装扮切换后主界面素材不换](./日志证据/2026-09-20_装扮切换后主界面素材不换.md) - 会话 `_dkck7pp6wxc4zu`：点主题后主界面画刷一个都没换；根因是 `DressConfigTable` 里 `Dress.SameName.ControlNames`（空值全局兜底行）与 `Dress.SameName.ControlNames.Hub`（14 控件清单）**点号路径前缀冲突**，前缀行把子树覆盖成空串 → `Get 中间节点不是table` → `GetSameNameControlNames count=0` → `ApplySameNameStyle 该界面无同名映射`；且依赖 `pairs` 顺序时好时坏。修复：删除前缀行（112→111）+ `Get` 报错补全冲突节点 + `SetNestedConfigValue` 加两类冲突告警；全表扫描 0 冲突。配套工具 `check_config_prefix_conflict.py`。未重新 PIE

- [2026-09-22_结算界面布局解析失败_前向引用与控件契约](./开发记录/2026-09-22_结算界面布局解析失败_前向引用与控件契约.md) - 13.09 双人 PIE「结算界面右侧仓库不为 6 列、覆盖层不落地」。**根因①（主因）**：`AuctionSettlementUIService.lua` 里 `ApplySettlementScrollGeometry` 定义在使用点之后（806 调用 / 870 定义）→ 运行时为 nil → `attempt to call a nil value (global 'ApplySettlementScrollGeometry')` ×122 → `ResolveSettlementOverlayLayout 出口 success=false` → `BuildSettlementOverlays 等待有效几何` → 36 个背景格从未被 `LayoutSettlementGridCells` 重排到滚动内容画布，只能由 WrapBox 按 `WrapWidth=728`（每格实占 122，6 格需 732）折行 → **视觉不是 6 列**。改为「L752 前向声明 + L875 赋值」修复（不能保留 `local function`，否则新建同名 local）。**根因②**：`REQUIRED_WIDGET_NAMES` 列了蓝图里根本不存在的 `AssetTitleText`、`LabelExpImage`（MCP 回读：蓝图变量 34 个、控件树 123 个，均无这两项；`Script/Blueprint/.../AuctionSettlementUI.lua:56` 仍留 `---@field LabelExpImage`）→ `ValidateWidgetContract` 恒失败 → `Initialize` 提前 return → `BindEvents`/`ActiveWidget` 不生效；已剔除（31→29 项）。自检：luaparser OK、全工程 75 个 Lua 前向引用 0 处、契约 29/29 ⊆ 控件树；**必须重新 PIE（双客户端），本轮未实机验证**
- [2026-09-22_结算覆盖层创建失败_结算网格行数配置错配](./开发记录/2026-09-22_结算覆盖层创建失败_结算网格行数配置错配.md) - 修完前向引用后 **13.37 会话**结算仍不正常：客户端 LuaLog 暴涨到 33.9MB+32.5MB（约 11 倍），`BuildSettlementOverlays` 在 `Tick` 里被重试 **188 次**。**根因**：结算网格「总行数」被配成 **6**，而竞拍仓库真实网格是 **6 列 × 14 行**（DS 硬证据 `PlaceShape 入口 gridWidth=6 gridHeight=14`；MCP 回读 `Gameplay.Warehouse.GridRows=14` vs `AuctionGlobalConfigTable.UI.SettlementWarehouse.GridRows=6`）——填表时把「可见行数 6」误填进「总行数」（`UIConfigTable.UI.SettlementWarehouse.VisibleRows=6` 已独立承担该语义）。代码回退值本身是对的（`AuctionSettlementUIService.lua:47` 回退 `AuctionConfig.GetGridHeight()`=14），被显式行覆盖。后果：`InitializeWarehouseGrid gridSize=6x6 cells=36`，`CreateOverlayWidgetForItem` 判据 `cellY < gridRows` 把 Y∈[6,11] 的占格全丢弃（实测 `忽略无效格 X=0 Y=6 / X=5 Y=9 / X=2 Y=11`，**X 恒在 0..5 即列数正确**）→ 26 件只建成 16 件 → `覆盖层创建不完整 createdCount=16 itemCount=26` → `PendingOverlayBuild` 不清除 → 每帧重试。**已确认第三轮 RPC 分片修复生效**（`SendWarehouseChunks sent=8 failed=0`、`MergeWarehouseChunk chunkIndex=4 chunkTotal=4 itemCount=26 complete=true`），`Cells` 数据完整到达，纯属坐标与行数不匹配。修复：P0 配置 `UI.SettlementWarehouse.GridRows` 6→14（或删行走代码回退）+ `GridSlots` 36→84；P1 代码 `CreateOverlayWidgetForItem` 越界降级不丢弃整件、`BuildSettlementOverlays` 加重试上限防日志风暴、`GetSettlementMetrics` 加一致性告警。**P0+P1 已落地**：配置 `GridRows=14`/`GridSlots=84`（MCP 回读确认）+ 新增 `UIConfigTable.UI.SettlementWarehouse.MaxOverlayBuildAttempts=3`；Lua 改 5 处（越界 clamp 降级 / 重试上限降级放行 / ShowNextItem 无覆盖层跳过 / GetSettlementMetrics 尺寸一致性告警 / 常量注释订正），改后 1860 行 113358 B，luaparser OK、11 处标记回读全中、75 个 Lua 前向引用 0 处。**必须重新 PIE（双客户端），未实机验证**。踩坑：`data_table_add_row` 返回 `None` 而非 `True`，只能靠回读确认
- [2026-09-20_装扮主题资源清单与替换映射](./配置表/2026-09-20_装扮主题资源清单与替换映射.md) - **装扮主题资源全量梳理（方案产出，未实施接入）**：磁盘递归枚举 `Asset\TuPian`（212 个 `.uasset`）+ MCP `ue_py` 遍历 13 个界面蓝图控件树导出素材→控件绑定 + `UIConfigTable` 58 条贴图路径 `ue.load_object(Texture2D)` 校验。交付 `工程 docs/Dress_Theme_Asset_Mapping.json`（`modules` 13 / `configRows` 13 / `controlRows` 66 / `pathFixes` 43）与表格版 `Dress_Theme_Asset_Inventory.md`。**核心结论**：① 用户提的 4 个模块实际要拆成 **13 个 UiKey**（目录是替换映射的自然边界），`Codex`/`CodexItem` **共用 `TuJian` 目录**；② **推翻旧认知**——竞拍素材实际在 `AuctionUI/Auctioninterface/` 而非 `AuctionUI/` 直下（旧快照过时），且 **`ZhuJieMian_TestTopic` 目录实际已存在**（13 文件），此前"未创建"记录不准确、TestTopic 文件齐备性仍未验证；③ 使用率 115 磁盘 / 83 使用中 / 17 未引用 / 4 缺文件，冗余集中在 Settlement(5)、AuctionSkill(5)、Codex(3)、Hub(ZJM2/ZJM4)，`*Reference` 是设计稿留档建议排除；④ `UIConfigTable` 58 条仅 15 条可加载：9 条需补 `Auctioninterface/`、31 条需补 `/IslandAuctionKing/` 前缀、3 条真缺失（`HouseArt_IslandContainer/MountainArmory/AirdropSecret`）；⑤ 待补 = 12 个 `<BaseFolder>_TestTopic` 目录 + 约 83×12 张变体图 + 3 张真缺失图 + 43 条配置行 + **12 处脚本接入点**（当前仅 `AuctionHubUIService` 调了 `ApplySameNameThemeToHostUIWhenReady`）。建议首批只做 Hub(11)+Auction(7)+Settlement(15)=33 张验证链路
- [2026-09-20_装扮控件映射总表](./配置表/2026-09-20_装扮控件映射总表.md) - **装扮控件映射重构后终态（已写入表）**：清掉 4 条不存在的 SlotKey 控件行（`Dress.Control.Hub.PlayerAvatar` / `Dress.Control.Hub.PlayerTitle` / `Dress.Control.Auction.AuctionSeat` / `Dress.Control.Settlement.SettlementWhole`）与 2 条失效配置行（`Dress.SameName.TextureNames` / `Dress.MaterialTablePath`）；按四界面八 UiKey 重建 **109 行** `Dress.SameName.Control.<UiKey>.<控件名>`（Hub 14 / Auctioninterface 17 / Price 1 / Skill 11 / Info 1 / RoundDetail 14 / Settlement 12 / Codex 39）+ 8 组 `ControlNames.<UiKey>` 与 `BaseFolder.<UiKey>`。事实源为四个 UMG 蓝图 WidgetTree 递归导出。RoundDetail 为本轮新增 UiKey，**尚未登记进 `AuctionConfig.DressThemeTablePaths`**（仍 7 张表），故装扮 UI 里暂不出现该分类。
- [2026-09-20_装扮主题文件夹表与装扮UI改造](./配置表/2026-09-20_装扮主题文件夹表与装扮UI改造.md) - **装扮分类主题表（每个分类一张，共 7 张）与装扮 UI 改造（已实施、未 PIE）**：终态为 `DressThemeHubTable` / `DressThemeAuctioninterfaceTable` / `DressThemePriceTable` / `DressThemeSkillTable` / `DressThemeInfoTable` / `DressThemeSettlementTable` / `DressThemeCodexTable`，各 **14 列**（`Category/CategoryName/CategorySortOrder` + `SubCategory/SubCategoryName/SubSortOrder` + `Folder/ThemeFolder/Preview` + `ThemeId/ThemeName/UnlockState/UnlockTip/SortOrder`，**列注释已中文化**），行名 `Dress.Theme.<UiKey>.<ThemeId>`；**目前只有默认主题 DefaultGray 一行**（当前 UI 图片，目录即基目录不加后缀），其余主题素材做好后逐表补行。主题文件夹**为分类独有**、解锁状态按「分类 × 主题」独立配置。分类目录：主界面 `ZhuJieMian`、竞拍 4 个二级（`Auctioninterface`/`Price`/`Skill`/`Info`）、结算 `AuctionUI/Settlement`、图鉴 `TuJian`。**UiKey 采用二级标识**（Hub/Auctioninterface/Price/Skill/Info/Settlement/Codex），以后写 `Dress.SameName.BaseFolder.<UiKey>` 须用这组名，与资源清单 JSON 里的 `Auction/AuctionPrice` 那套需对齐。代码：`AuctionConfig` 用**通用装载器** `LoadDressCustomTable(path, logTag, columns, requiredColumns)` 装载 7 张同构表（**专用装载**——自定义列没有 `Value`，走不了 `LoadOneDressTable`），`BuildDressCascade` 聚合出「一级→二级」分类树，对外 `IsDressThemeTablesLoaded()` / `GetDressCascade()` / `GetDressThemeRows(uiKey)`；`AuctionDressUIService` 分类与主题按钮**全部读这 7 张表**（未装载时内置兜底）、**材质列表移除**、右侧 `MaterialButton%02d` 槽位**复用为主题按钮**（未解锁置灰+文案加「（未解锁）」），预览按当前分类×主题的 `ThemeFolder` 走 `LoadSameNameTexture`。**删除废弃表** `DressMaterialTable` + 行结构（依据：全工程仅 1 处路径声明、0 处读取；`objed_delete_asset` 报 not found，**`ue.delete_asset` 才成功**）。luaparser 4/4 通过。踩坑：建表前须 `objed_open_editor('DataTable')`（09-18「三入口全失败」本次未复现）；**遍历中 `struct_remove_variable` 会漏删**（删 4 列只掉 3 列，须回读补删）；改列前先清空行；空单元格须 `nil`/`0` 双判空
- [2026-09-16_工程外置资料迁出知识库（辅助文件索引）](./开发记录/000_辅助文件索引.md) - 把工程内 `Preview`(112 文件/8,314,384 B)、`Script\Tools`(2/8,969)、`.workbuddy`(4/208,453)、`_ImportStaging\AuctionUI`(18/1,164,156) 整体迁到 `备份\IslandAuctionKing\20260916_工程外置资料迁移\`，把 `ConvertUAssetToJson.ps1` 整理封装为 `工具\UAssetToJsonConverter\dist\UAssetToJsonConverter.exe`，把 `_JsonOutput`(201/254,526,155) 迁到 `raw\IslandAuctionKing\_JsonOutput\`；逐文件 SHA256 + `LastWriteTime` 校验全部一致后才删除源目录；exe `--selftest` `ok=true`、`--mode convert` 实测 `total=284 converted=151 skipped=133 failed=0 cleaned=0`。含 6 条辅助文件登记项（AUX-20260916-001..006）与调用入口。踩坑：**PowerShell 5.1 按 GBK 解析 UTF-8 无 BOM 的 `.ps1`，中文路径字面量乱码并把文件写到乱码目录**，脚本化文件操作改用 Python；GUI 子系统 exe 必须 `Start-Process -Wait -PassThru` 才拿得到退出码

- [2026-09-16_选择与道具组数据持久化与滑动框定位](./开发记录/2026-09-16_选择与道具组数据持久化与滑动框定位.md) - 修复「角色选择/道具组选择/购买情况/金币/组内道具使用情况」五个持久化缺口 + 道具组滑动框自动定位。**根因**：①`SavePlayer` 用 `GetPersistentSelected*` 做授权过滤，`Test.EnableAllSelections=true` 时非默认角色与未购道具组一律回落默认 → 选择必丢 ②`player.UsedProps` 从未进存档 ③选择/购买/使用/激活后无即时落盘 ④`ActivateSelectedPropGroup` 无条件清空 `UsedProps`。`UGCPlayerController` 新增 3 个 FString 变量 `AuctionSelectedCharacter`/`AuctionSelectedPropGroup`/`AuctionGroupPropUsage`（由 `SyncControllerOwnership` 写）；存档新增字段 `GroupPropUsage`；新增 `AuctionPlayerService:SavePlayerImmediate`，在 `AuctionGameService` 的 SelectPropGroup(638)/BuyPropGroup(683)/SelectCharacter(736)/StartPreparing激活(779)/UseProp(1059)/SettleWinner(1287) 六处调用。滑动框：新增局部函数 `AuctionPropSelectUIService.ScrollSelectedGroupIntoView`（Refresh 末尾调用，先按 `(index-1)*206` 算偏移 `SetScrollOffset`，再 `ScrollWidgetIntoView(target,false,0)` 兜底，`AuctionPropSelectAutoScrollIndex` 防覆盖手动滚动）。新增配置行 5 条（UIConfigTable 3 条 `UI.PropSelect.*`、Gameplay 表 `Gameplay.ResetGroupPropUsageOnActivate=false`、全局表 `Test.PersistTestGrantedSelection=true`）。**记录 MCP 蓝图变量类型坑：`blueprint_add_member_variable(bp,name,"str")` 会生成 int32，正确类型串是 `"string"`**；已建错须用 `v.set_field("VarType", 同类型变量VarType)` 修复。luaparser 语法 OK + MCP 回读通过；**必须重新 PIE，本轮未实机验证**。15:15 追加：补齐**客户端下发**——`CF_FWDRPC.ClientAuctionPrivateState`（CF_FWDRPC.lua 87-100 行）新增 `AuctionSelectedPropGroup`/`AuctionSelectedCharacter`/`AuctionGroupPropUsage` 三行赋值（数据源是已下发的 `AuctionPrivateState.SelectedPropGroup/SelectedCharacter/UsedProps`，排序口径对齐服务端 `SyncControllerOwnership`），并顺带把 `UsedProps` 回退 `previousState`；仅函数体改动，**可热更新**，建议重新 PIE
- [2026-09-16_竞拍仓库藏品线索动效全覆盖](./开发记录/2026-09-16_竞拍仓库藏品线索动效全覆盖.md) - 工程内「线索」= 情报（Intel/InfoType）。补齐四类零动效缺口：①**开局/每轮角色技能线索**（`AuctionCharacterSkillService.ExecuteForPlayer` `mainRound==1` 走 `InitialTargetCount` → `LastSkillIntel`，此前只有 `SkillIntelText` 文本）②公共情报（`RefreshClientPublicIntelFeedback` 主动 `warehouseCellFeedbackSuppressed=true`）③道具 Shape/Full（`suppressSingleCell` 分支）④道具 Aggregate（`Scope=="Warehouse"` 被跳过）。新增 `AuctionClueFeedbackService` 编排层；`AuctionUIMotionService` 新增 `PlayClueFeedbackMotion`/`PlayClueFlashPhase`/`PlayClueWaveMotion`（**单 Tween 驱动多目标错峰波浪**）/ `StopClueMotion`。强度≥品质闪烁：PopScale 1.45(旧1.35)、FlashCount 4(旧3)、FlashMin 0.28(旧0.45)，另加飞入+高亮底色；**不调 `SetColorAndOpacity`** 规避 FSlateColor 坑。UIConfigTable 新增 19 行 `UI.Motion.ClueFeedback.*`（plan_id `plan_16792454_89042d0f`，回读 `MISSING=[]`）；**必须重新 PIE**
- [2026-09-16_结算界面自适应对齐竞拍界面](./蓝图与UI/2026-09-16_结算界面自适应对齐竞拍界面.md) - 以 `AuctionTestUI`（竞拍界面）自适应方案为标准修复 `AuctionSettlementUI`：根 `RootScaleBox` 的 `Stretch`/`StretchPc` 由 `5`（ScaleToFill，会裁切非 16:9 视口的顶部标题/底部按钮）改为 `2`（ScaleToFit，等比留边），`StretchDirection`/`StretchDirectionPc` 保持 `0`（Both）；MCP 写入、编译保存、关闭/重新打开资产后回读一致；含跨 8 档视口的交互式网页预览页 `Preview/SettlementAdaptiveCompare_20260916.html`；**需重新 PIE**
- [2026-09-16_竞拍仓库藏品占格重叠日志分析](./日志证据/2026-09-16_竞拍仓库藏品占格重叠日志分析.md) - DebugID `dkck7plomtr75a`：揭晓阶段 12 件藏品出现 1 对图片压盖（85.2×85.2，正好一格）。**服务端占格数据无冲突**。**藏品表 70 行 Shape 全是矩形（无 L 型）**，MCP `data_table_as_dict` 回读确认；非矩形**只来自回退生成器** `BuildConnectedShape`（`width=min(6,ceil(sqrt(n)))`，n∈{3,5,7,8} 出 L 形）。本次 item12 防水帐篷包表格 `3×1` 竖条经 120 次放置失败后走 `SlotCountPlacementFallback` → `BuildConnectedShape(3)` → `width=2 actualCells=3` L 形（DS 硬证据）；客户端 `AuctionTestUIService.RefreshWarehouse` 只按 min/max 包围盒铺整图（939-953 行）且藏品层无显式 ZOrder → 缺角压住 item1 的 (1,1)。全天统计：回退调用 25 次，非矩形 2 次（8%）。另发现表格 4 行 `Shape 面积≠SlotCount`（折叠侦察无人机 2×3=6 vs 5、时空裂隙样本 3×4=12 vs 5 必出 L 形；便携净水器、反重力碎片 安全）。判据 `包围盒格数 > validCells`。修复首选 **D 放置失败先旋转**（`RotateCells` 已实现但 `quarterTurns` 被硬编码 0）+ A 回退改矩形优先 + E 修表格 + C ZOrder 兜底；需重新 PIE；**本轮未改工程**
- [2026-09-15_AuctionTestUI_竞拍界面概念图部件拆分](./蓝图与UI/2026-09-15_AuctionTestUI_竞拍界面概念图部件拆分.md) - 按 `ue.widget_inspect` 回读的 `AuctionTestUI` 当前控件树（6 大板块 + 6 浮动面板，1920x1080 舞台），把修仙风竞拍界面概念图（1920x1156，含 76px 水印条）拆成 20 个部件并切图；含「概念图区域 ↔ 控件」映射表与 6 条差异/待确认（席位槽位 4 vs 5、顶栏右侧三小框当前无控件、符文石板为单块底纹而非 36 格、坐标 ±10~20px 需设计器复核）；产物在 `备份\IslandAuctionKing\20260915_竞拍界面部件拆分\`
- [2026-09-15_仓库反馈动效_高亮闪烁渐显弹出](./开发记录/2026-09-15_仓库反馈动效_高亮闪烁渐显弹出.md) - 竞拍中右侧仓库区域加载完成并出现仓库反馈时播放组合动效：`AuctionUIMotionService` 新增 `PlayWarehouseFeedbackMotion`（弹出+渐显 → 高亮闪烁 N 次 → 复位）与 `StopWarehouseFeedbackMotion`；`AuctionWarehouseFeedbackService.ApplyClientFeedback` 用 state `MotionPlayed` 保证 0.25s 重刷不重播，五处恢复格子处调 Stop 复位；`UIConfigTable` 新增 7 行 `UI.Motion.WarehouseFeedback.*`（293→300，plan `plan_16804300_46bf3a45`，回读全对）；luaparser 语法 OK、预览 `Preview/AuctionWarehouseFeedbackMotionPreview.html`；**必须重新 PIE**
- [2026-09-15_保底金配置补档与动效句柄修复](./开发记录/2026-09-15_保底金配置补档与动效句柄修复.md) - **真因：`GuaranteeConfigTable`（10 行）早已存在，但 `AuctionConfig.LoadFromTable` 从未加载它**，保底金阈值/次数/模式与 `UI.Guarantee.*` 全部走 Lua 回退常量；新增 `AuctionGuaranteeConfigTableService` + 注册 `AuctionConfig.GuaranteeConfigTablePath`（`Data/Table/Customized/Guarantee/GuaranteeConfigTable`）；表补 6 行到 16 行（plan_id `plan_16802875_3def686c`）；`UIConfigTable` 补 `UI.Hub.DressFailedText`（plan_id `plan_16802905_e6fdf571`）；`AuctionUIMotionService` 的 Press/Fade/CloseToken 三处句柄改 Lua 弱键缓存 `MotionState`，消除 UButton 上 `AuctionMotionPressHandle property not exist`；PIE DebugID `dkck7pkt2b82xq` 复验：客户端 `appliedCount=16 invalidCount=0`、DS `guaranteeConfigLoaded=true`、连点 3 次异常计数 0
- [2026-09-15_主界面接入保底金UI打开功能](./开发记录/2026-09-15_主界面接入保底金UI打开功能.md) - `AuctionHubUIService` 新增 `OpenGuaranteeGold`（第 493-517 行），`BindEvents` 里 `OpenGuaranteeButton` 回调从 `OpenUnavailable("GuaranteeText")` 改为 CreateAndShow；UIConfigTable 新增 `UI.Hub.GuaranteeFailedText`（plan_id `plan_16800751_50c3e13a`，回读通过）；luaparse 语法 OK；**PIE 复验通过**（DebugID `dkck7pkt2b60fk`，16:55 `CreateAndShow success=true zOrder=520 bindOK=true`，重复点击走 AlreadyExists）；原待办（`Guarantee.*`/`UI.Guarantee.*` 未进表、`UI.Hub.DressFailedText` 缺失、`PlayPress` 报 property not exist）已由同日 [2026-09-15_保底金配置补档与动效句柄修复](./开发记录/2026-09-15_保底金配置补档与动效句柄修复.md) 全部修复
- [2026-09-15_AuctionDressUI_装扮UI优化写入与验收](./蓝图与UI/2026-09-15_AuctionDressUI_装扮UI优化写入与验收.md) - 装扮 UI 优化落地：新增 13 个装饰/文案控件（控件数 73→86，不改名不删）、21 控件几何逐项精确（`GEOM_BAD=[]`）、三列 `VerticalBoxSlot.SizeRule` 0→1 修行高失效（末位空槽 Value=0）、`DressScaleBox StretchPc` 0→2 修 PC 不缩放；`contract=69 missing=[]`；plan_id `plan_16792485_3a4adc98`；写前备份 MD5 `A301849B3AEC0984BD9958B522548356` → 写后 `A363269184E67FED3FDB6434392569AA`（90,248→179,869 B）；截图核验未命中目标画面（`capture_fail_*.png` 留证）；**必须重新 PIE**
- [2026-09-14_Data与UI目录结构全量清单](./资产清单/2026-09-14_Data与UI目录结构全量清单.md) - `Asset/Data`（6 文件夹/29 文件）、`Asset/Blueprint/Prefabs/UI`（1/11）、`Asset/Blueprint/Prefabs/Items`（1/1）完整树状快照 + 中文名对照；表格侧为 `Table/Customized/<功能域>/<功能域>ConfigTable` 三段式，UI 侧单层平铺；MCP 实测 `Prefabs/Items` 是物品编辑器「物品」类型落盘目录（212 模板，同级 `Weapons`/`Throwables`/`MeleeWeapons` 对应枪械/投掷物/近战武器），新增物品应走 `objed_create_asset('Item','物品',…)` 而非手工放 `.uasset`；用 `FolderTreeTranslator v1.3.0` 三项目实测（`IslandAuctionKing` 100% / `pet_paradise` 96.6% / `SearchInfectionZone` 87.5% 中文名完整率），含拼音识别引擎（音节切分+词级拼音表）与型号保留规则
- [2026-09-15_PIE主界面不铺满_根ScaleBoxStretch修复](./蓝图与UI/2026-09-15_PIE主界面不铺满_根ScaleBoxStretch修复.md) - PIE 下主界面只铺满 0.828×0.828 的修复：根缩放框 `CanvasPanel_0_Wrapper_Wrapper` 的 `Stretch`/`StretchPc` 由 0 恢复为 2（ScaleToFit），并删除 `AuctionHubUIService.ApplyViewportFit` 中手写 `SetRenderScale`+`SetPositionInViewport` 的 `DynamicScaleToFit` 分支（UMG 渲染空间与物理像素存在视口缩放差，与根 ScaleBox 叠加会二次缩小）；plan_id `plan_16784780_1b93aa0e`；模板匹配量化 0.828×0.828 → 0.997×1.000；必须重新 PIE
- [2026-09-14_ZhuJieMian主界面底图导入与设置](./蓝图与UI/2026-09-14_ZhuJieMian主界面底图导入与设置.md) - 主界面整屏底图控件确认为 `参考图`(UImage，四向拉伸/ZOrder=-5)；复采时画刷为 `None`（2026-08-26 基线记 `ZJM2` 已过期，控件树 23→46）；导入 `ZJMBackground`(1671x941) 并写入 UI 五项合规属性、`SetBrushFromTexture` 绑定；plan_id `plan_16790507_30f41bfa` / `plan_16790539_1d9b62f4`；运行期无覆写（装扮系统未实现 Target 应用）；必须重新 PIE
- [2026-09-10_UIConfigTable_UIMotion行](./配置表/2026-09-10_UIConfigTable_UIMotion行.md) - UIConfigTable 新增 6 行 UI.Motion.*；plan_id plan_16796695_640bc434；data_table_find_row 回读 ValueType/Value 通过；必须重新 PIE
- [2026-09-10_道具选择ItemNames顿号字节拆名](./日志证据/2026-09-10_道具选择ItemNames顿号字节拆名.md) - 客户端 LuaLog：道具表 40 行 Name 读到；UI 用 [、] 字节类把「品」拆成「微型??/鉴仪器」，介绍为空；可热更新
- [2026-09-10_道具组默认未拥有与控制器金币拥有态](./开发记录/2026-09-10_道具组默认未拥有与控制器金币拥有态.md) - UGCPlayerController 新增 AuctionGold/AuctionOwnedPropGroups/AuctionOwnedProps；默认空库存；购买 RPC 扣费写拥有态；plan_id plan_16782954_b1bee8c3；必须重新 PIE
- [2026-09-10_AuctionCharacterSelectUI_CharacterDetailText自动包裹](./蓝图与UI/2026-09-10_AuctionCharacterSelectUI_CharacterDetailText自动包裹.md) - CharacterDetailText 的 Wrapping 分组未勾选自动包裹文本；本轮只记录截图，未改蓝图
- [2026-09-16_仓库滚动框与6x14网格](./蓝图与UI/2026-09-16_仓库滚动框与6x14网格.md) - 竞拍+结算各加 WarehouseScrollBox/ContentSizeBox/ContentCanvas；仓库 6×14=84 格、藏品 24~32；竞拍背景格运行时动态创建（AuctionWarehouseCellService），失败回退蓝图固定格；AuctionTestUI 藏品槽补到 32 组；订正 LayoutData 必须走 SetPosition/SetSize UFunction；必须重新 PIE
- [2026-09-09_AuctionPropSelectUI_道具行状态底与道具图](./蓝图与UI/2026-09-09_AuctionPropSelectUI_道具行状态底与道具图.md) - 新增 ItemStatusBg01-06 绑 PropSelectStatus；道具名居中进灰条；客户端读 AuctionPropTable 上道具图；plan_id plan_16803388_5b5d4b91 / plan_16804243_0659ba30；必须重新 PIE
- [2026-09-09_AuctionPropSelectUI_滑动框与文字归位](./蓝图与UI/2026-09-09_AuctionPropSelectUI_滑动框与文字归位.md) - 新增 GroupScrollBox/ItemScrollBox；17 组卡与 6 道具行 AddChild 进滑动框；组名/剩余/道具名/状态改为卡内坐标；删除叠字 GroupDesc/ItemDesc；plan_id plan_16789478_c325edb2 / plan_16794304_61b2beb7 / plan_16799290_33c27973；必须重新 PIE
- [2026-09-09_AuctionPropSelectUI_切图绑定与拥有态](./蓝图与UI/2026-09-09_AuctionPropSelectUI_切图绑定与拥有态.md) - 按确认后的网页预览绑定技能道具切图；底栏价格与购买/补全/已拥有；右侧道具行按拥有态切图；plan_id plan_16782227_96e060da / plan_16784601_48ef66c0 / plan_16786676_5498b120；必须重新 PIE
- [2026-09-11_ZhuJieMian会场匹配区用户修正锚点](./蓝图与UI/2026-09-11_ZhuJieMian会场匹配区用户修正锚点.md) - 用户手动修正后 MCP 只读：会场簇 11 个控件 Min=Max=(1,0.5)；覆盖 2026-09-10 误抄金币条 (1,0) 的结论；本轮未改工程
- [2026-09-11_ZhuJieMian_HUD收回1920画布](./蓝图与UI/2026-09-11_ZhuJieMian_HUD收回1920画布.md) - SizeBox/Canvas ClipToBoundsAlways(3)，SizeBox 对齐改 Fill；贴边 HUD 不再撑出 1920 虚线框；plan_id `plan_16779251_24e9a52b`；必须关页签再开
- [2026-09-10_ZhuJieMian设计态None运行时ScaleToFit](./蓝图与UI/2026-09-10_ZhuJieMian设计态None运行时ScaleToFit.md) - ScaleToFill 裁切顶栏与会场；设计态 Stretch=None，PIE SetStretch(ScaleToFit=2)；plan_id `plan_16805313_98564093`；必须重新 PIE
- [2026-09-10_ZhuJieMian_ScaleToFill铺满大分辨率](./蓝图与UI/2026-09-10_ZhuJieMian_ScaleToFill铺满大分辨率.md) - 根改为 ScaleBox+SizeBox 1920x1080，Stretch=ScaleToFill(5)；设计器多分辨率对照不再把 1920 布局缩在左上角；plan_id `plan_16803752_9a40e533`；必须重新 PIE
- [2026-09-10_ZhuJieMian视口铺满与占比修复](./蓝图与UI/2026-09-10_ZhuJieMian视口铺满与占比修复.md) - ApplyViewportFit 改为四向拉伸铺满视口，取消 1920 小卡片缩放；LuaLog dkck7pgf7nuy5b 读到 view=1280x720 scale=0.67；必须重新 PIE
- [2026-09-10_ZhuJieMian会场匹配区右上锚点](./蓝图与UI/2026-09-10_ZhuJieMian会场匹配区右上锚点.md) - 当时把会场簇写成右上 (1,0)；2026-09-11 用户手动改为右中 (1,0.5)，该页像素不再当基线
- [2026-09-08_ZhuJieMian锚点恢复与服务器倒计时](./蓝图与UI/2026-09-08_ZhuJieMian锚点恢复与服务器倒计时.md) - 恢复贴边点锚点并新增倒计时；2026-09-10 按 20269 包裹 ScaleBox+SizeBox 1920x1080 ScaleToFit，外层四向拉伸，里层坐标未改；plan_id `plan_16778672_20d386f0` / `plan_16779025_8fa3ec14`；必须重新 PIE
- [2026-09-08_AuctionHouseSelectUI_选场后大厅乐观写入场名](./开发记录/2026-09-08_AuctionHouseSelectUI_选场后大厅乐观写入场名.md) - SelectHouse 成功后乐观写入 VenueNameText 为「当前：场名」，再 Refresh，再回写，再 Hide；金币不足不写大厅；必须重新 PIE
- [2026-09-08_AuctionPropSelectUI_确认后大厅乐观写入组名](./开发记录/2026-09-08_AuctionPropSelectUI_确认后大厅乐观写入组名.md) - Confirm 成功后乐观写入 PropTitleText/PropSummaryText 为已选组名，再 Refresh，再回写，再 Hide；未改大厅服务与坐标；必须重新 PIE
- [2026-09-08_大厅开始匹配改为官方二次匹配](./开发记录/2026-09-08_大厅开始匹配改为官方二次匹配.md) - 大厅「开始匹配」改为客户端 UGCMultiMode.RequestMatch；服务端未进入竞拍子模式拒绝假开局；未启动 PIE，未勾选 Multi-mode Game
- [2026-09-08_AuctionPropSelectUI_PIE乱码与点击修复](./蓝图与UI/2026-09-08_AuctionPropSelectUI_PIE乱码与点击修复.md) - 第二轮按字段重写 TitleText/CloseLabel/ConfirmLabel/PropHeadingText Roboto Bold；Refresh 写 CloseLabel；新增 UI.PropSelect.CloseText=X；plan_id `plan_16793050_e39e5834` / `plan_16793182_9781a15d`；必须重新 PIE
- [2026-09-08_AuctionHouseSelectUI_PIE乱码与点卡选场](./蓝图与UI/2026-09-08_AuctionHouseSelectUI_PIE乱码与点卡选场.md) - 资产条/关闭/场名/kv/hint/LOCKED 只 set_field 恢复 Roboto Bold；新增透明 HouseHit01-03 点卡选场；金币不足禁用点击层；未改现有坐标；plan_id `plan_16791429_7be4d0b1`；必须重新 PIE
- [2026-09-07_AuctionPropSelectUI_对齐网页预览仪器组合层](./蓝图与UI/2026-09-07_AuctionPropSelectUI_对齐网页预览仪器组合层.md) - 删除测试壳并按 `#propOverlay` 重建米色 1280x760 仪器组合层；MCP plan_id `plan_16791955_e0b3069a` / `plan_16792287_3e42b331`；未启动 PIE
- [2026-09-07_AuctionPropSelectUI_删除角色残留控件](./蓝图与UI/2026-09-07_AuctionPropSelectUI_删除角色残留控件.md) - 编辑器设计器把 Collapsed 且无贴图的 UBorder 画成色块；用 MCP 删除复制残留的 CharacterPanel/CharacterButton01..06 等 10 个控件并回读；plan_id `plan_16788216_6d421b4b`
- [UI 贴图全量合规清单](./资产清单/2026-08-26_UI贴图全量合规清单.md)
- [竞拍 UI 贴图导入与运行时替换](./资产清单/2026-08-31_竞拍UI贴图导入与运行时替换.md) - 11 张竞拍界面素材、独立 UIConfigTable、8 个 UBorder 运行时画刷绑定及 PIE 回读证据
- [UGCPlayerController 竞拍 UI 图片路径结构体](./蓝图与UI/2026-08-31_UGCPlayerController竞拍UI图片路径结构体.md) - `AuctionUITexturePathSet` 结构体、`AuctionUITexturePaths` 控制器成员、Lua 读取与局内切换入口
- [竞拍 UI 布局与按钮画刷修复](./蓝图与UI/2026-08-31_竞拍UI布局与按钮画刷修复.md) - `AuctionTestUI` 参考图坐标、主框架/玩家卡/仓库内框落位、`UButton.WidgetStyle` 四态贴图和 PIE DS 阻塞证据
- [UI 贴图批量修正 MCP 回读](./日志证据/2026-08-26_UI贴图批量修正MCP回读.md)
- [ZhuJieMian 画刷基线说明](./蓝图与UI/2026-08-26_ZhuJieMian画刷基线说明.md)
- [图鉴UI素材批量导入与视觉优化](./开发记录/20260824_图鉴UI素材批量导入与视觉优化.md) - 从 raw/docs/ai 完整分类副本
- [FlaUI 编辑器完整窗口截图](./蓝图与UI/2026-08-27_FlaUI_编辑器完整窗口.png) - FlaUI GDI 捕获，1936x1056，PNG 验证通过
- [图鉴 UI 编辑器元件位置截图](./蓝图与UI/2026-08-27_图鉴UI编辑器元件位置截图.md) - FlaUI 进入 UI 编辑器“元件”页，确认两个图鉴元件可见
- [图鉴藏品数据与详情打开修复](./日志证据/2026-08-31_图鉴藏品数据与详情打开修复.md) - DebugID `dkck7sb0t99tqi`，记录图鉴卡片初始化失败、贴图字段不匹配与 Lua 修复
- [2026-09-04_仓库打开图鉴筛选形状顶栏未跟随](./蓝图与UI/2026-09-04_仓库打开图鉴筛选形状顶栏未跟随.md) - 仓库聚焦时顶栏筛选形状改读 knownFilter，与左侧形状高亮一致
- [2026-09-07_图鉴关闭挂点残留与右下角元素消失](./蓝图与UI/2026-09-07_图鉴关闭挂点残留与右下角元素消失.md) - 图鉴改用官方 `AddToSlot(Widget,SlotName,ZOrder)` 与关闭前 `RemoveFromSlot`；右下角花盆不属本项目资产，待查证为和平原生元素
- [2026-09-04_结算仓库名称框溢出修复](./蓝图与UI/2026-09-04_结算仓库名称框溢出修复.md) - 结算仓库复用图鉴卡片时收起 NameFrameImage/ItemArt 并开启 ClipToBounds，空 WarehouseGrid 折叠；未启动 PIE
- [2026-09-04_结算放大镜灰块与估价文本修复](./蓝图与UI/2026-09-04_结算放大镜灰块与估价文本修复.md) - 揭示动画用 GetWidgetFromName 收起未导出的 ItemArt，恢复右侧 EstimatedValueText/SellValueText；未启动 PIE
- [2026-09-10_AuctionSettlementUI_切图替换](./蓝图与UI/2026-09-10_AuctionSettlementUI_切图替换.md) - 结算 UI 按参考图换切图，删除正在展示文本控件，动态金额改金色数字；MCP 回读通过，未启动 PIE
- [2026-09-11_AuctionSettlementUI_设计态白块修复](./蓝图与UI/2026-09-11_AuctionSettlementUI_设计态白块修复.md) - 折叠空头像画刷与右栏空容器，仓库标题收到 1288,36 / 380x48；plan_id `plan_16798125_26953c2a`；必须关页签再开
- [2026-09-11_AuctionSettlementUI_切图原尺寸居中](./蓝图与UI/2026-09-11_AuctionSettlementUI_切图原尺寸居中.md) - 底板改回 141,60 / 1639x960，不再硬拉满；折叠烘焙标题与空头像；plan_id `plan_16802023_96246791`；必须关页签再开
- [2026-09-11_AuctionSettlementUI_铺满1920x1080](./蓝图与UI/2026-09-11_AuctionSettlementUI_铺满1920x1080.md) - 用户纠正后覆盖居中方案；底板 0,0 / 1920x1080，标题图和空头像保持折叠；plan_id `plan_16803991_65372dc2`；必须关页签再开
- [2026-09-01_PIE启动黑屏与DS崩溃日志分析](./日志证据/2026-09-01_PIE启动黑屏与DS崩溃日志分析.md)
- [2026-09-10_匹配加载后仍显示主界面](./日志证据/2026-09-10_匹配加载后仍显示主界面.md) - DebugID dkck7pgf7nkitq/dkck7pgf7nkitr；二次匹配已进 1002 并开拍，客户端 BeginPlay 按 Waiting 重建主界面，OnRep 未触发，StartBidRound RPC 只 Refresh 不关大厅
- [2026-09-10_匹配加载后仍显示主界面修复](./开发记录/2026-09-10_匹配加载后仍显示主界面修复.md) - 新增 AuctionClientPhaseUIService；ClientAuctionPublicState 缓存早到状态并按阶段关大厅；Lua 语法通过，未启动 PIE
- [2026-09-10_使用道具后仍可查看介绍](./开发记录/2026-09-10_使用道具后仍可查看介绍.md) - 本轮已用道具后剩余槽仍可点开介绍；确定使用仍禁用；可热更新
- [2026-09-10_竞拍结束后返回主界面](./开发记录/2026-09-10_竞拍结束后返回主界面.md) - 结算关闭后 RequestMatch(1001)；FinishGame 不再 GameOver 踢回和平大厅；建议重新 PIE
- [2026-09-04_PIE上传文件名校验失败](./日志证据/2026-09-04_PIE上传文件名校验失败.md) - DebugID dkck7sej3cjjhq，根目录 _tmp_lua_balance.py 触发 CheckName：must begin with an alphabetic character - DebugID `dkck7sbwdrpw7r`，确认 DS 在 `UGCGameModeBase::InitGame` 断言崩溃，客户端首包握手超时
- [2026-09-14_编辑器连续闪退日志分析](./日志证据/2026-09-14_编辑器连续闪退日志分析.md) - 至少 7 个 UE4 native Crash 包；移走 `AIGC_Terrain.bin` 后仍闪退；自动恢复资产/布局、Slate/Texture 路径和编辑器内置资源版本风险；具体 native 调用栈待查证
- [2026-09-15_AuctionTestUI_WidgetOuter断言修复](./日志证据/2026-09-15_AuctionTestUI_WidgetOuter断言修复.md) - 定位并修复 6 个 `RoundDetailContent` 子控件的 `Outer` 错误；MCP 重建、编译保存、全 UI 回读和编辑器打开通过；PIE 上传验证通过但后端调试请求失败
- [2026-09-15_保底金领取卡在领取中_RPC白名单缺注册](./日志证据/2026-09-15_保底金领取卡在领取中_RPC白名单缺注册.md) - DebugID dkck7pkt2b60fk，DS 确认 `CF_KHDRPC+Dispatch 错误 非白名单函数 ServerRPC_ClaimGuaranteeGold`；修复：CF_KHDRPC.lua 新增薄包装并注册 AllowedHandlers；需重新 PIE
- [2026-09-16_线索动效Tween重复Kill断言闪退](./日志证据/2026-09-16_线索动效Tween重复Kill断言闪退.md) - 客户端 DebugID dkck7plomtzdy8；`Assertion failed: Pair != nullptr [Containers/Map.h:586]` → 弹 Assert 框卡 5 秒 → `appError` 闪退；根因 AuctionUIMotionService 36 个 state 共享同一 Tween 句柄被逐个 KillTween（IsTweenValid 拦不住）；修复：新增 KillSharedClueHandle 组去重 + 完成回调改 ResetClueVisual；luaparser 通过、行号回读一致。**2026-09-20 双人 PIE 复现（已补录）**：修复代码在位（日志可见 `KillSharedClueHandle` / `ResetClueVisual` 在跑）的情况下该断言**仍发生**（DebugID `_dkck7pp6wwoo9p` 两客户端 11:04:48.162 / 11:04:48.956，1.5 分钟后点 Ignore → `appError`），定位到 09-16 **漏改**的同类路径 `PlayClueFlashPhase`（`AuctionUIMotionService.lua` L821 只写 `ClueHandle` 不写 `ClueGroup` → L827 完成回调仍调 `StopClueMotion` → 日志 `shared=false` 绕过去重 → 对**刚自然完成**的 tween 再 KillTween）；建议 1 行修复 L827 `StopClueMotion`→`ResetClueVisual`（**未实施**，改前须备份、改后须重新 PIE）
- [2026-09-15_AuctionGuaranteeGoldUI_画刷ResourceObject与Slot对齐修复](./蓝图与UI/2026-09-15_AuctionGuaranteeGoldUI_画刷ResourceObject与Slot对齐修复.md) - 保底金弹窗「UI超出画布、图片素材未使用」四个真因定位与修复：`CanvasPanelSlot.LayoutData.Alignment` 被 widget_move 重置为 (0.5,0.5) 导致面板左上偏移、13 处画刷 `ResourceObject` 误存为 `UPackage` 导致 Slate 回落白贴图、缺根 `ScaleBox→SizeBox(1920x1080)→ContentCanvas` 包裹层、**`RootSizeBox` 的 `WidthOverride/HeightOverride` 写了但 `bOverride_*` 重载开关未打开（EditCondition）导致运行时内容被放大 1.264 倍并裁切**（`Stretch` 5→2）；MCP 回读 + 设计器截图 + 彩色探针 + PIE 运行时绘制区量测（行间距 170.67px→126.33px）验证通过
- [2026-09-01_AuctionTestUI_删除角色技能入口](./蓝图与UI/2026-09-01_AuctionTestUI_删除角色技能入口.md) - 通过 MCP 删除 `OpenSkillPanelButton`/`OpenSkillPanelLabel`，清理 Lua 绑定并完成 PIE 复测
- [2026-09-01_AuctionTestUI_第二张参照图对齐](./蓝图与UI/2026-09-01_AuctionTestUI_第二张参照图对齐.md) - 通过 MCP、UIConfigTable 和运行时贴图替换将竞拍界面对齐第二张空白框架参照图，并完成 PIE/FlaUI 复测
- [2026-09-01_AuctionFunctionalPreview_完整功能预览](./蓝图与UI/2026-09-01_AuctionFunctionalPreview_完整功能预览.md) - 按现有竞拍素材制作网页预览，验证报价、锁定、藏品选择、清空及底部功能入口；用户确认后已继续蓝图功能恢复
- [2026-09-01_AuctionTestUI_素材导入与网页预览对齐](./蓝图与UI/2026-09-01_AuctionTestUI_素材导入与网页预览对齐.md) - 导入技能、说明、价格输入素材，完成 UIConfigTable 配置及 AuctionTestUI 设计态与网页预览对齐；最终对齐后未重新 PIE
- [2026-09-01_AuctionTestUI_编辑器设计态与网页预览对比](./蓝图与UI/2026-09-01_AuctionTestUI_编辑器设计态与网页预览对比.md) - MCP 写入主框架 UBorder 画刷、删除刷新/清空旧控件、纠正底部栏 TopBottomBar 与报价区 FooterBid 分层；根 UI 保持 1620x971 设计舞台，最新按需求改为 Fill 铺满展示区域；本轮未启动 PIE
- [2026-09-02_AuctionTestUI_画布溢出修复](./蓝图与UI/2026-09-02_AuctionTestUI_画布溢出修复.md) - 将根 `ScaleBox_0.Stretch` 从 `Fill(1)` 恢复为 `ScaleToFit(2)`，保留 `1620x971` 设计舞台；完成 MCP 回读和 FlaUI 编辑器截图验证，未启动 PIE
- [2026-09-02_AuctionTestUI_网页预览红框美化](./蓝图与UI/2026-09-02_AuctionTestUI_网页预览红框美化.md) - 按 `AuctionFunctionalPreview.html` 基线修正红框布局、字号、文字、色板、按钮画刷与轮次槽背景；MCP 回读 20 个轮次按钮和设计态截图验证，未启动 PIE
- [2026-09-03_AuctionTestUI_席位123对齐席位4](./蓝图与UI/2026-09-03_AuctionTestUI_席位123对齐席位4.md) - 以 P4 相对标题偏移对齐 P1-P3 的序号/图片/出价/点击区；MCP 回读 mismatch_count=0，FlaUI 设计态截图验证，未启动 PIE
- [2026-09-03_AuctionTestUI_席位3绿框红框对齐](./蓝图与UI/2026-09-03_AuctionTestUI_席位3绿框红框对齐.md) - 按参照图绿框/红框把 P3 五格道具图和点击区落到席位3暗槽；P4 未改，未启动 PIE
- [2026-09-03_AuctionTestUI_仓库网格收回暗槽](./蓝图与UI/2026-09-03_AuctionTestUI_仓库网格收回暗槽.md) - 将 CellButton31-36 收回 WarehousePanel 暗槽，移走左侧 Collectible 叠加绿块，并同步 UI.TestWarehouse 配置；未启动 PIE
- [2026-09-03_AuctionTestUI_绿框情报红框仓库收敛](./蓝图与UI/2026-09-03_AuctionTestUI_绿框情报红框仓库收敛.md) - 按用户绿框/红框收缩 RightDetailPanel 与 WarehousePanel，并重排情报卡与 6x6 仓库格；未启动 PIE
- [2026-09-03_AuctionTestUI_仓库格缩小与预估价回标题行](./蓝图与UI/2026-09-03_AuctionTestUI_仓库格缩小与预估价回标题行.md) - 将 6x6 格子收回仓库暗槽，并把 WarehouseText 预估价移回标题行；未启动 PIE
- [2026-09-03_AuctionTestUI_仓库格落入用户红框](./蓝图与UI/2026-09-03_AuctionTestUI_仓库格落入用户红框.md) - 按用户红框缩小并下移 6x6 CellButton，同步 UI.TestWarehouse；未启动 PIE
- [2026-09-03_AuctionTestUI_二级面板按网页预览与素材回收](./蓝图与UI/2026-09-03_AuctionTestUI_二级面板按网页预览与素材回收.md) - 将轮次详情/报价键盘/说明/道具列表收回网页预览与导入素材像素尺寸；未启动 PIE
- [2026-09-03_AuctionTestUI_轮次详情设计态贴图绑定](./蓝图与UI/2026-09-03_AuctionTestUI_轮次详情设计态贴图绑定.md) - 将 RoundDetailPanel 设计态空画刷绑定到已导入轮次详情素材，收起旧副标题/安全说明白文案；未启动 PIE
- [2026-09-03_AuctionTestUI_说明面板设计态贴图绑定](./蓝图与UI/2026-09-03_AuctionTestUI_说明面板设计态贴图绑定.md) - 将 HelpPanel 设计态空画刷绑定 InfoPanel/箭头，并把正文字面 \\n\\n 改成真实换行；未启动 PIE
- [2026-09-03_AuctionTestUI_价格输入面板设计态贴图绑定](./蓝图与UI/2026-09-03_AuctionTestUI_价格输入面板设计态贴图绑定.md)
- [2026-09-04_AuctionTestUI_初始次级面板折叠](./蓝图与UI/2026-09-04_AuctionTestUI_初始次级面板折叠.md) - 进入竞拍主界面时折叠 BidKeypadPanel/PropPanel，避免设计态 Visible 直接露出来 - 将 BidKeypadPanel 绑定 PriceInput，透明点击层对齐格子，清除键收回第四列；未启动 PIE
- [2026-09-03_AuctionTestUI_道具栏对齐左侧席位红框](./蓝图与UI/2026-09-03_AuctionTestUI_道具栏对齐左侧席位红框.md) - 将 PropPanel 贴到 LeftPlayerPanel 红框几何并绑定 SkillPanel；未启动 PIE
- [2026-09-03_AuctionTestUI_道具栏顶部详情绑定SkillBar](./蓝图与UI/2026-09-03_AuctionTestUI_道具栏顶部详情绑定SkillBar.md) - 为 PropPanel 红框详情区补 SkillBar 底图和左侧道具图；未启动 PIE
- [2026-09-03_竞拍主玩法脚本静态体检](./蓝图与UI/2026-09-03_竞拍主玩法脚本静态体检.md) - 主玩法 Lua/RPC 静态体检；修复 PhaseText 必填契约导致竞拍 UI 初始化失败；未启动 PIE
- [2026-09-03_AuctionTestUI_道具选中勾移到行右侧绿框](./蓝图与UI/2026-09-03_AuctionTestUI_道具选中勾移到行右侧绿框.md) - 将 PropSelectedMark 从整行左对齐收到行右侧绿框；选中时显示；未启动 PIE
- [2026-09-03_AuctionTestUI_全席位按席位3绿框红框对齐](./蓝图与UI/2026-09-03_AuctionTestUI_全席位按席位3绿框红框对齐.md) - 将 P3 绿框/红框槽位模板复制到 P1-P4，并下移 P3 标题与 P1 间距对齐；未启动 PIE\n- [2026-09-01_AuctionTestUI_轮次详情素材导入与对齐](./蓝图与UI/2026-09-01_AuctionTestUI_轮次详情素材导入与对齐.md) - 导入轮次详情五张组件素材，新增 RoundDetailContent 视觉层、居中文案与独立配置路径；本轮未启动 PIE
- [AuctionPropGroupTable Itemimage 脚本读取](./配置表/2026-09-04_AuctionPropGroupTable_Itemimage脚本读取.md) - 组表 object 列 Itemimage_3_50AE95F24A04FF034A7E73BF7E690DDC，脚本优先读表图并回退品质占位；17 行当时为空\n- [角色道具选择未跳转竞拍日志分析](./日志证据/2026-08-28_角色道具选择未跳转竞拍日志分析.md)
- [2026-09-04_选择完成后竞拍UI未打开](./日志证据/2026-09-04_选择完成后竞拍UI未打开.md) - DebugID dkck7sej3cle9p，选择 RPC 已成功，AuctionTestUI Construct 因 Button_384 为空失败 - DebugID `dkck7s8e3q4evu`、`dkck7sb0t8yfxy`，确认客户端 `KHDRPC_Call` 被拒绝导致选择未到达 DS
- [2026-09-22_结算仓库滚动截断与跳过估值未收敛](./开发记录/2026-09-22_结算仓库滚动截断与跳过估值未收敛.md) - ①滚动到底仍截断：脚本用属性赋值写 `WarehouseContentSizeBox.HeightOverride` 不会同步 Slate，滚动范围仍是设计态 1239（≈10.15 行），改走 `SetHeightOverride` 并同步蓝图设计态到 728×1704；②跳过展示后左侧估值不收敛：`SkipShowcase` 只写右侧，补写 `AccumulatedValue`/`ActualValueText`/`ProfitText`。需重新 PIE 验证
- [2026-09-22_结算藏品展示顺序改为按仓库位置](./开发记录/2026-09-22_结算藏品展示顺序改为按仓库位置.md) - 展示顺序由 `SortByQuality`（品质优先）改为按仓库位置：新增 `ResolveItemAnchorCell`（锚点=左上角占格）+ `SortWarehouseItems`，顺序由配置行 `UIConfigTable.UI.SettlementWarehouse.ShowcaseSortMode` 控制（`RowMajor` 行优先默认 / `ColumnMajor` 列优先 / `Quality` 旧行为）。需重新 PIE 验证

---

## 五、检索方式（按需索引）

```powershell
# 只在本项目资料内检索
rg -n "关键词" "D:\知识库\和平精英绿洲起源\raw\IslandAuctionKing"

# 通用知识检索（不含任何项目专属目录）
rg -n "关键词" "D:\知识库\和平精英绿洲起源\wiki" "D:\知识库\和平精英绿洲起源\raw\docs"
```

---

## 六、待查证

1. `raw/docs/ai/20260824_图鉴UI素材批量导入与视觉优化.md` 是否需要在后续版本物理搬迁到本目录，取决于是否愿意同步改写 wiki 侧的 raw 引用路径，**尚未与需求方确认**。

- [2026-09-07_拍卖场选择UI对齐网页预览](./蓝图与UI/2026-09-07_拍卖场选择UI对齐网页预览.md) - 按网页预览 #houseOverlay 重排 AuctionHouseSelectUI；第二轮删除测试页残留；第三轮修复 FontObject=None 乱码；第四轮绑定地图选择底板/卡框/标题条贴图，底板 280,97/1361x875，三卡 430/805/1180,306/309x433；未启动 PIE
- [2026-09-07_AuctionCharacterSelectUI_对齐网页预览](./蓝图与UI/2026-09-07_AuctionCharacterSelectUI_对齐网页预览.md) - 按网页预览 #characterOverlay 重建角色选择层：中板 1380x760@270,160、橙色顶栏「角色选择」、3x2 肖像格、右侧姓名/定位/技能/确认；widget_remove 测试标题/道具组/翻页/底栏；新增 UI.CharacterSelect.* 37 行；未启动 PIE，FlaUI 枚举不到提权编辑器
- [2026-09-09_AuctionCharacterSelectUI_切图绑定](./蓝图与UI/2026-09-09_AuctionCharacterSelectUI_切图绑定.md) - 按桌面人物选择参考图导入 8 张切图并绑定到底板、六卡头像框、右侧详情与确认钮；未启动 PIE
- [2026-09-08_AuctionCharacterSelectUI_PIE乱码与点卡无详情](./蓝图与UI/2026-09-08_AuctionCharacterSelectUI_PIE乱码与点卡无详情.md) - 标题/关闭/确认保持 Roboto Bold 中文；肖像绑 WhiteSquareTexture 实色遮罩；新增 UI.CharacterSelect.TitleText/CloseText/ConfirmText；必须重新 PIE 验证
- [2026-09-08_角色选择确认后大厅无反馈](./蓝图与UI/2026-09-08_角色选择确认后大厅无反馈.md) - Confirm 成功后 Refresh 大厅并乐观写 CharacterTitleText；Hide 放在 Refresh 之后；未改大厅服务与坐标；必须重新 PIE 验证
- [2026-09-08_ZhuJieMian主界面点击层与大厅功能接入](./蓝图与UI/2026-09-08_ZhuJieMian主界面点击层与大厅功能接入.md) - 给 ZhuJieMian 补透明点击层并接入三个选择层、仓库图鉴、帮助与未开放提示；MCP 回读四个必选按钮存在，UI.Hub.* 19 行写入通过；未启动 PIE
- [2026-09-08_ZhuJieMian用户摆正后布局回读](./蓝图与UI/2026-09-08_ZhuJieMian用户摆正后布局回读.md) - 用户手动摆正后 MCP 只读回读 44 个控件的 Anchors/Offsets；匹配文字从估算 Y=668 移到 432；右下三按钮仍是左上角锚点，未改 Lua/uasset
- [2026-09-08_ZhuJieMian大厅头像名称金币加载](./蓝图与UI/2026-09-08_ZhuJieMian大厅头像名称金币加载.md) - 红框三处：GoldValueText 保持右上 -250,36 并恢复 Roboto Bold 22；PlayerNameText 落到底左胶囊；头像框保持空框，官方未确认玩家头像 API；未启动 PIE
- [2026-09-08_PIE等待阶段未创建ZhuJieMian](./日志证据/2026-09-08_PIE等待阶段未创建ZhuJieMian.md) - 最新 PIE 日志只有「等待阶段保留主界面大厅，不创建竞拍测试UI」，没有 ZhuJieMian+Construct；已补 AuctionHubUIService.CreateAndShow 并在 GameState/PlayerController 等待阶段调用
- [2026-09-21_仓库藏品图被格子遮挡_GetName异常回退遗留格](./蓝图与UI/2026-09-21_仓库藏品图被格子遮挡_GetName异常回退遗留格.md) - ApplyCellChrome GetName 抛异常致 84 格创建中断、回退遗留 36 格遮挡滚动藏品；SafeGetWidgetName+孤儿格清理+覆盖层每帧重申 ZOrder；需重 PIE
- [2026-09-22_仓库藏品覆盖层19-32未迁移与UI同名行冲突](./蓝图与UI/2026-09-22_仓库藏品覆盖层19-32未迁移与UI同名行冲突.md) - 蓝图实为 32 组覆盖层，EnsureOverlays 却按回退配置 18 扫描，19..32 共 42 控件既未迁移也未折叠、被 z=10 滚动框盖住；同名行 AuctionGlobalConfigTable=18 覆盖 UIConfigTable=32，另查出结算偏移 24/158 覆盖 40/146，两处重复行已删（153→150）；顺带移除本引擎恒失效的 GetName/GetClass 兜底，84 格省约 76 秒；需重 PIE
- [2026-09-22_揭晓阶段RPC超限断连_裁剪与分片修复](./开发记录/2026-09-22_揭晓阶段RPC超限断连_裁剪与分片修复.md) - **「两人都锁定报价后连接超时」根因与修复**：揭晓瞬间 `ClientAuctionPublicState`（28 件 × 13 字段 = **6656 元素**）与 `ClientAuctionPrivateState`（**4290 元素**）同时超过 `net.MaxRepArraySize(4096)` → `FlushError` + `Assertion failed:false` → **DS 停摆表现为断线**（`Saved\Crashes` 无新目录，非客户端崩溃）。修复（5 文件）：揭晓藏品裁到 6 字段、私有 `Collectibles` 只留 `ID`/`Name`（图鉴只消费这两个）、新增分片 RPC `ClientAuctionWarehouseChunk`（主包带元信息 + `ChunkTotal`，客户端 `MergeWarehouseChunk`/`ReplayWarehouseChunks` 按版本累积去重，收齐后重刷仓库与估价）、结算 RPC 加空明细兜底、发送前体积自检（阈值 3000）。luaparser 5/5 OK、标记回读 23/23；备份在 `备份\IslandAuctionKing\20260922_RPCArraySizeLimit\before\`。**未重新 PIE**；待办：`Network.WarehouseChunkSize` / `Network.MaxRepSafeElements` 两行配置需确认落哪张表（无 Network 域表）
- [2026-09-10_全0流拍不打开结算UI](./日志证据/2026-09-10_全0流拍不打开结算UI.md) - 最新 PIE 会话 AllZeroNoSale 不发送 ClientAuctionSettlementUI，Ended 时 settlementOpen=false 回大厅；对照 13:33 Sold 局 RPC 与 OpenSettlementUI 均有执行证据
- [2026-09-22_仓库藏品遮挡第四轮_裁切方案证伪](./日志证据/2026-09-22_仓库藏品遮挡第四轮_裁切方案证伪.md) - **方案 B（调裁切模式）实测证伪**：`SizeBox.Clipping=0` / `ContentCanvas.Clipping=0` 本就是 `Inherit`，链路上无多余裁切空间；`ScrollBox.Clipping=1` 是唯一裁切层。**同时推翻两条旧结论**：① `Slot:GetZOrder()` 完全生效（覆盖层读到 `40`、底格读到 `10`，与 `overlayZOrder=40`/`baseCellZOrder=10` 一致，首子 z=10→末子 z=40 排序正确）；② 本引擎 `CanvasPanel` **无** `bExplicitCanvasChildZOrder`（日志 `PropName [bExplicitCanvasChildZOrder]: property not exist`，四个候选字段全 nil）。`ScrollBox` 直接子控件数 `1`（只有 SizeBox），内容画布子控件 `148`、`SizeBox=562.2x1300.7`、`sbOffset=0.0`（未滚动）。**现象与「ZOrder 已正确」仍矛盾，真实根因未定**；排除范围与下一步见页内 §5。方法学：`doluastring` **不能用 `\n`**（只回显不执行）、**片段过长被静默丢弃**、返回 `{}` ≠ 成功必须读日志、PIE 会自行停止
- [2026-09-22_仓库藏品遮挡第五轮_真凶ItemArt锁定](./日志证据/2026-09-22_仓库藏品遮挡第五轮_真凶ItemArt锁定.md) - **遮挡问题最终闭环（已修复 + PIE 验证通过）**。真凶 = 单格 `CollectibleCodexItemUI_C` 内的 **`ItemArt`**：`UBorder` 无贴图、`BrushColor` 深灰 **A=1.0**、`Pos(18,12) Size(224,141)`，是 `ItemContent` 的**最后一个子控件** → 压住全部兄弟（含 `ItemImage`），表现为「藏品图被格子盖住」。**根因是未勾选 `bIsVariable`**（Lua 取不到 → `ApplyCellChrome` 折叠不了），且它在**单格内部**而非画布层级，故前四轮针对 ZOrder / 覆盖层迁移 / 裁切的修复必然无效。修复：蓝图勾选 `ItemArt.bIsVariable`（`33714→33670` B），**无需改 Lua**；PIE 实测 `collapsedByName=9 unreachable=[]`、活体 `ItemArt=1(Collapsed)`（改前为 `NIL`）。**两条新引擎事实**：① `ESlateVisibility` 本引擎 `Visible=0/Collapsed=1/Hidden=2`，**与标准 UE 对调**；② `GetWidgetFromName` **只挂 UUserWidget 根控件**（UMG 原生控件上 `property not exist`）。方法学：探针先确认对象是谁再读值；**JSON 镜像会过期，判「属性缺失」前先比 mtime**；读 `Slot` 要区分自己的与父级继承的；**单格内部遮挡看子控件顺序即可，不必查 ZOrder**
- [2026-09-22_仓库藏品遮挡第六轮_像素级取证与ZOrder失效定论](./日志证据/2026-09-22_仓库藏品遮挡第六轮_像素级取证与ZOrder失效定论.md) - **用户复报「问题还是未解决」后，本轮改用受控变量实验 + 像素级截图取证，推翻前五轮的整条修复思路。** **核心定论：本引擎 `Slot:SetZOrder` 数值读写完全生效（回读 40/10），但它不驱动实际绘制顺序** —— 四次实验：① 基线覆盖层被格子盖住；② 84 格全部 `SetVisibility(Collapsed)` → 覆盖层**立即显现**；③ 覆盖层 z 由 40 改为 **5**（低于格子 10）→ **仍被遮挡**（证明 z 与遮挡无关）；④ 84 格 `ItemButton` 背景 `A` 改 0 → 整片暗方格**整体消失**。补充实验 D：`RemoveFromParent` + `AddChildToCanvas` 真正重挂到末尾 → **仍被遮挡**（证明绘制顺序也**不是**子控件插入顺序）。**遮挡源已锁定**：底格 `CollectibleCodexItemUI_C` 的 `ItemButton` 背景刷 `EMPTY_COLOR = {0.03,0.04,0.06, A=0.92}`（**92% 不透明**，`AuctionTestUIService.lua:71/906`）；第五轮折叠 `ItemArt` 确实有效，但同一格内还有这层 92% 不透明背景，两层叠加只解决一层 → 现象依旧。**覆盖层自身逐项核查全部正常**（`Visibility=3 HitTestInvisible` / `ColorAndOpacity=(1,1,1,1)` / `RenderOpacity=1.0` / `brush.ResourceObject=Texture2D a8.a8` / `brush.ImageSize=32x32` / `brush.DrawAs=3`）。**新引擎事实**：`ESlateBrushDrawType` 本引擎序号 = `NoDrawType=0 / Image=3 / Box=1 / Border=2`（**标准 UE 不同**，判读 `DrawAs` 必须按本表，否则会把正确的 `Image(3)` 误读成 `Box/Border`）。**取证方法**：`ctypes.EnumWindows` 按 `PIEPlayer_T1_10001` 取 hwnd + `PIL.ImageGrab.grab(bbox, all_screens=True)` 截图，网格细节再 `crop + resize(2x, LANCZOS)` 放大；跳过完整对局流程可直接 `AuctionTestUIService.CreateAndShow(gameState)` 打开被测界面。**修复方向待用户定夺**（A 调低格背景 `A`；B 改为把藏品图写进格内 `ItemImage`；C 格子迁到独立下层容器；D 先定论引擎绘制顺序规则）。
- [2026-09-22_结算正确而竞拍仓库错误_层级机制对照](./日志证据/2026-09-22_结算正确而竞拍仓库错误_层级机制对照.md) - **用户提问「为什么结算界面藏品图片在格子上面、而竞拍仓库的在格子下面？」后，本轮以「同引擎现成对照组」做代码级定论，并完成第七轮代码修复。** **对照结论**：两处使用**同一个单格控件** `CollectibleCodexItemUI_C`，差异只在「怎么用」——（1）**结算**（`AuctionSettlementUIService.CreateOverlayWidgetForItem:1093-1118`）用 `NewWidgetObjectBP` **新建控件实例**并 `AddChildToCanvasSafe` **追加到画布末尾**，图片因此天生是「最后追加的独立兄弟」，**完全不依赖 z**；（2）**结算背景格**（同文件 `:646-653`）显式 `ItemButton:SetBackgroundColor({R=0,G=0,B=0,A=0})` **彻底清零**；（3）**竞拍仓库**（`AuctionWarehouseCellService.EnsureOverlays:566-588`）用的是**蓝图预置** `CollectibleImage01..32`，而 `AddChildToCanvas` **对已在同一父容器内的控件不改变相对顺序** → 图片仍排在格子之前 → 被盖；（4）**竞拍仓库背景格底色从不清零**，且被三层不透明来源覆盖：蓝图设计态默认 **`A=1.0 R=1.0`（纯白，活体实测）**、`GRID_TEXTURE_TINT_COLOR={1,1,1,A=1.00}`（纹理已应用）、`EMPTY_COLOR={0.03,0.04,0.06,A=0.92}`（回退分支）。**第七轮工程定论**：本引擎 CanvasPanel **绘制顺序 = 父容器子控件插入顺序（后者在上）**；抬层**唯一有效手段是先 `RemoveFromParent` 再 `AddChildToCanvas` 真正重挂末尾**（第六轮实验 D 失败正因未先摘除）。**顺序本身没问题（活体实测）**：`EnsureOverlayOrder` 报 `appliedCount=64 wrongCount=0 childCount=148 lastCellIndex=83`，148 = 84 格(idx 0..83) + 64 覆盖层(idx 84..147) → 顺序全对却仍被盖，**只可能是格子的不透明填充画在了更上层**。**⚠️ 时序陷阱（本轮新踩）**：`EnsureCells` 里写的 `A=0` 会被紧随其后的 `AuctionTestUIService.RefreshWarehouse:906` `button:SetBackgroundColor(gridBaseColor)` **覆写** → 白改；**正确落点在上游刷新处，不是 `EnsureCells`**。**本轮已落地代码改动**（`AuctionWarehouseCellService.lua`，luaparser 通过 + `reloadlua` 生效）：① `EnsureOverlays` 搬迁改为真正重挂并新增 `reattachedCount/appendedCount/appliedZOrder` 统计；② `EnsureOverlays` 的 `Cached` 快路径改为**先断言顺序**，异常时走 `RaiseOverlays` 纠正；③ `EnsureOverlayOrder` **整函数重写**——判据由不可靠的「回读 z」改为**按父容器子控件序号断言**（`GetChildrenCount` + `GetChildAt`，记录 `lastCellIndex` 与各覆盖层 idx）；④ `EnsureOverlayOrderAfterCells` 配套改判据文案（`OrderOK(...)`）；⑤ `EnsureCells` 新增 `ItemButton` 底色清零（须配合上游修复才生效）；⑥ `GetLayout` 注释纠错（`SetZOrder` 不驱动绘制顺序，仅语义保留）。**修复方案（已明确，两处缺一不可）**：① `AuctionTestUIService.RefreshWarehouse:906` 背景格乘色改 `{0,0,0,A=0}`；② `AuctionWarehouseImageMaterialService.GRID_TEXTURE_TINT_COLOR` 的 `A` 由 `1.00` 改 `0`。**运行环境坑**：PIE 与会话本轮**自行停止 3 次**（`is_debug_playing:false`），末条 `doluastring` 因此**根本没送达客户端**（日志里搜不到该 `dostring content` 行）→ **探针无回显时第一件事是查 `ctx:` 确认 PIE 是否还活着**；UI 被折叠时（本引擎 `Collapsed=1`）画面只剩结算界面，且重新 `SetForegroundWindow` 后枚举到的是**编辑器窗口**，须按标题含 `PIEPlayer` 精确匹配。
- [2026-09-22_第十五轮_手机端仓库偏移与图鉴点击修复](./日志证据/2026-09-22_第十五轮_手机端仓库偏移与图鉴点击修复.md) - **手机端双客户端 PIE 实测**：修复藏品图片布局前清零 `SetRenderTranslation(0,0)` / `SetRenderScale(1,1)`，并将透明藏品点击层由 `HitTestInvisible` 改为 `Visible`；`CollectibleButton01 visibility=0`，`OnCollectibleClicked` 与 `OpenForWarehouseItem` 均 `success=true`，图鉴筛选摘要 `形状=2×3 · 占格=6`。新增强制流程：启动 PIE 前先核对地图/玩法 `RequiredPlayers`，本项目必须两个客户端。
- [2026-09-23_仓库藏品图左偏移4px_ItemContent内缩修复](./日志证据/2026-09-23_仓库藏品图左偏移4px_ItemContent内缩修复.md) - **用户复报「藏品图片对比仓库格子会有一点向左偏移」后，定位并修复 4px 系统性左移**。量化：用户截图紫色主体 `x=13..68`、同列格子 `x=17..72`，等宽但左移 4px。排除外层槽位/贴图/渲染变换后，运行时探针（`GetWidgetFromName("ItemContent")`）实测 `ItemContent` 父级 `UButtonSlot` 早期为 `Padding=(4,2,4,2)`、`HAlign=2(Center)`、`VAlign=2(Center)`。修复：`AuctionWarehouseCellService.lua` 新增 `NormalizeItemContentSlot`，`SetPadding(0)` + `HAlign_Fill/VAlign_Fill`，在 `EnsureCells` 的 Cached 与 Created 两条路径都调用（不用固定 +4px 补偿）。验证：luaparser 通过；双客户端手机端 PIE（DebugID `_dkck7prtmha7vq`）回读 `pad=0,0,0,0 align=0,0`、84 格 `applied=true`；像素复测 T1 `999 vs 1000`、T2 `935 vs 937`，4px 偏移消失。**归因边界**：本轮 `paddingBefore` 因 Cached 路径先归一化而读为 0，故可确定 `Center -> Fill` 修复了系统性偏移，不能单独证明 4px 全部由 Padding 贡献。待办：结算界面复用同一单格控件但尚未归一化 `ItemContent` 槽位。**取证要点**：`ue_pie` 的 `client_hwnd` 必须是字符串；未勾 Is Variable 的控件须用 `GetWidgetFromName`。
- [PIE调试启动要求_双客户端](./工具与流程/PIE调试启动要求_双客户端.md) - **本工程强制约定**：PIE 必须 `team_count=2`（根因 `AuctionConfig.RequiredPlayers = 2`），单客户端进不去竞拍且客户端会自行退出（`No PIE client window is registered` / `Lua reload was not handled`）。含双客户端窗口句柄获取方法（Python312 `ctypes` + `EnumWindows` 按 `PIE` 标题枚举，实测 `PIEPlayer_T1_10001` / `PIEPlayer_T2_20001`）与本轮 4 条 `doluastring` 踩坑。**自行开启 PIE 前必须先查项目要求**
