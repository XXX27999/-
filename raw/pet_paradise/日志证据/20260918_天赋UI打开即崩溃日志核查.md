# 天赋UI（TianFu）打开即崩溃日志核查（2026-09-18）

- **类型**：日志证据 / 问题排查
- **主题**：`Script/Blueprint/Prefabs/UI/TianFu/TianFu.lua` 的 `Construct` 抛 Lua 异常导致天赋界面完全不可用
- **适用范围**：项目 `pet_paradise`（绿洲起源编辑器工程）天赋系统客户端 UI
- **证据状态**：**项目实测**（PIE 客户端 LuaLog / TagLog / FullLog，2026-09-18 11:20:36，DebugID `dkck7pnfrvp5w7`）+ 源码定位（TianFu.lua 106/107/122/839 行）
- **更新时间**：2026-09-18
- **关联主题**：[十模块 UI 动效统一接入](../开发记录/2026-09-17_十模块UI动效统一接入.md)、[UI 动效方案与 Tween 落地](../蓝图与UI/2026-09-17_UI动效方案与Tween落地.md)
- **排除范围**：服务端天赋升级逻辑（`CF_KHDRPC.ServerRPC_UpgradeTalent`，本次日志中未被执行，未参与本次异常）

---

## 一、结论（一句话）

`TianFu:Construct()` 第 107 行先调 `self:InitMotion()`，而 `InitMotion` 第 839 行拼接 `self.SelectedTalentImageName .. "_1"`；该字段要到 `Construct` 第 122 行才赋值 `"TX"`。Construct 首行即抛 `attempt to concatenate a nil value`，**后续 `LuaInit`（绑定事件/属性）、`RefreshTalentDisplay`、`SelectTalent` 全部未执行**——天赋界面打开即失效。

源码依据（工程内绝对路径 `D:\WeGameApps\rail_apps\OasisEraEditor(2001776)\ShadowTrackerExtra\UGCProjects\pet_paradise\Script\Blueprint\Prefabs\UI\TianFu\TianFu.lua`）：

| 行号 | 代码 | 说明 |
| --- | --- | --- |
| 106-107 | `function TianFu:Construct()` / `self:InitMotion()` | 动效初始化被放在 Construct 第一行 |
| 122 | `self.SelectedTalentImageName = "TX"` | 选中态字段在这里才赋值 |
| 839 | `local SelectedInd = self[self.SelectedTalentImageName .. "_1"]` | 崩溃点：拼接 nil |

---

## 二、关键证据（日志原文）

### 2.1 客户端 LuaLog（主证据）

文件：`D:\WeGameApps\rail_apps\OasisEraEditor(2001776)\ShadowTrackerExtra\Saved\Logs\pet_paradise\Clientlog\LuaLog\2026.09.18-11.14.11_client__dkck7pnfrvp5w7_1.log`

```
[2026.09.18-11.20.36:373] LogNula: LuaLog: GlobalData.OnUAEUserWdigetNativeConstruct WidgetPath:/pet_paradise/Asset/Blueprint/Prefabs/UI/TianFu/TianFu.TianFu_C IsInLobby:false
[2026.09.18-11.20.36:376] LogUGCClient: [TagLog] 【UIMotionKit+PlayListStagger】列表入场项数 8，步长 0.04
[2026.09.18-11.20.36:385] LogTagLog: Error: [TagLog] [LuaException] type=LuaPCallFailed
    msg=CallLuaUFunction Failed: UFuncSig=[Construct]
    info=Script/Blueprint/Prefabs/UI/TianFu/TianFu.lua:839: attempt to concatenate a nil value (field 'SelectedTalentImageName')
stack traceback:
    Script/Blueprint/Prefabs/UI/TianFu/TianFu.lua:839: in method 'InitMotion'
    Script/Blueprint/Prefabs/UI/TianFu/TianFu.lua:107: in function <...TianFu.lua:106>
    [C]: in method 'AddToViewport'
    Script/Blueprint/Prefabs/UI/OpenShop.lua:648: in function <...OpenShop.lua:627>
```

### 2.2 客户端 TagLog / FullLog（同源，各 1 条 / 4 条命中）

- `Clientlog\TagLog\2026.09.18-11.14.11_client__dkck7pnfrvp5w7_1_TagLog.log`（1146、1148、1149 行）
- `Clientlog\FullLog\2026.09.18-11.14.11_client__dkck7pnfrvp5w7_1.log`（206022、206024、206025、206029 行）
- 调用链上游：`OpenShop.lua:648` → `AddToViewport` → `TianFu:Construct`，确认是从主界面「装扮/天赋」入口打开。

### 2.3 定向检索：天赋业务日志一条都没有

以 `【TianFu+HandleTalentUpgrade】`、`【CF_KHDRPC+ServerRPC_UpgradeTalent】`、`【TianFu+RefreshTalentDisplay】`、`【TianFu+SelectTalent】`、`【TianFu+Image_118_OnMouseButtonDownEvent】` 为标签全量扫描该会话，命中数 **0**。与「Construct 中断」互相印证：UI 未初始化 → 点击回调未绑定 → 升级按钮与服务端 RPC 都不会触发。

### 2.4 回归时间窗（项目实测推断）

- `TianFu.lua` 最后修改时间 **2026-09-17 17:32:41**，本次崩溃堆栈指向的 `InitMotion` 属同日 UIMotionKit 接入改动。
- `TianFu.lua:839` 与 `SelectedTalentImageName` 关键字在 **2026-09-17 全部会话日志中 0 命中**，仅在 2026-09-18 会话出现 → 9/17 改动后当天未实际打开过天赋界面，**2026-09-18 11:20 是首次触发**，属未验证就合入的回归。

---

## 三、次要信号（低优先级，非本次主因）

`ugc env:try to index /Script/UnrealTween.UnrealTweenConfig which does not exist in mt, maybe you want to f2ugc`

- 当前会话客户端 LuaLog 命中 65 条，最早出现在 **11:17:30**，早于天赋 UI 打开的 11:20:36；9/17 多个会话同样大量出现（168 / 411 / 459 条）。
- 属 **Warning** 而非 Error，跨会话长期存在，判定为引擎侧既有噪声。
- **待查证（纠正）**：`Script\Common\UI\UIMotionKit.lua` 第 27-34 行注释把该告警归因为「传入 `MakeConfig()` 产出的对象」。但 `UIMotionKit.UseTweenConfig = false`（第 34 行）已使 `GetConfig` 直接返回 nil、不再调用 `MakeConfig`，告警仍持续打印 → **该注释的因果归属不准确**，需重新确认告警真实来源（疑为 `UGCTweenSystem.TweenFloatValue/TweenVectorValue` 引擎侧自身行为）。此项不影响天赋崩溃结论。

---

## 四、修复（2026-09-18 已实施）

改动文件：`Script/Blueprint/Prefabs/UI/TianFu/TianFu.lua`
写前备份：`D:\知识库\和平精英绿洲起源\备份\pet_paradise\20260918_天赋系统日志排查\TianFu.lua.bak`（38075 字节，与改前一致）

1. **顺序修正（主修复）**：`Construct` 内把 `TalentCache = {}` / `SelectedTalentImageName = "TX"` / `SelectedTalentInfo = TALENT_ROW_DATA.TX` 三行从原 121-123 位置**上移到 `self:InitMotion()` 之前**（现 109-111 行），消除 nil 拼接。
2. **兜底（防同类复发）**：`InitMotion` 内改为 `local selectedName = self.SelectedTalentImageName or "TX"`，再取 `self[selectedName .. "_1"]`（现 841-842 行）。

### 验证（项目实测，非预估）

- `luaparser` AST 解析：`AST_PARSE_OK`（脚本 `D:\知识库\和平精英绿洲起源\备份\pet_paradise\20260918_天赋系统日志排查\verify_tianfu_patch.py`）。
- 关键串断言全过：`SelectedTalentImageName = "TX"` 1 处、`self:InitMotion()` 1 处、`selectedName .. "_1"` 1 处、旧的 `self.SelectedTalentImageName .. "_1"` **0 处**、`TalentCache = {}` 2 处（Construct 1 + `RefreshTalentDisplay` 原有 nil 守卫 1，合法共存）。
- 改后从磁盘重读 106-135 行、832-854 行核对，落盘正确。
- 工程内无 `.py` / `__pycache__` / `.pyc` 残留（已扫描）。

### 生效方式

涉及 UI 初始化（`Construct`），**需重新调试 PIE**，不可依赖热更新。

### 预估成功日志（未实机验证，仅预估）

打开天赋界面后：不再出现 `TianFu.lua:839: attempt to concatenate a nil value`；出现 `【UIMotionKit+PlayListStagger】列表入场项数 8` 与 `【TianFu+SelectTalent】` 且无 `[LuaException] UFuncSig=[Construct]`；点击升级后出现 `【TianFu+HandleTalentUpgrade】`，DS 侧出现 `【CF_KHDRPC+ServerRPC_UpgradeTalent】` 及其分支日志。

补充：`UIMotionKit.Release(self)` 已在 `TianFu:Destruct`（846-848 行）正确调用，符合「控件销毁前释放动效句柄」约定；但 Construct 中断后 `Destruct` 的触发时机需结合 `OpenShop.lua:648` 的创建路径一并确认（**待查证**）。

---

## 五、验证方式

改动后重新 PIE，打开天赋界面，预期日志（**预估成功日志，非实测**）：

1. 不再出现 `TianFu.lua:839: attempt to concatenate a nil value`；
2. 出现 `【UIMotionKit+PlayListStagger】列表入场项数 8` 之后紧跟 `【TianFu+SelectTalent】` 相关日志，无 `[LuaException] ... UFuncSig=[Construct]`；
3. 点击升级按钮后出现 `【TianFu+HandleTalentUpgrade】`，DS 侧出现 `【CF_KHDRPC+ServerRPC_UpgradeTalent】` 及其成功/失败原因分支日志。
