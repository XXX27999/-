# IslandAuctionKing PIE 上传文件名校验失败

> 类型：项目证据
> 主题：PIE / 上传文件名校验 / 下划线开头临时文件 / pycache 点号连字符
> 适用范围：IslandAuctionKing，DebugID `dkck7sej3cjjhq`，编辑器日志 2026-09-04 10:42
> 证据状态：项目实测；编辑器弹窗与 CheckName 失败已确认；挪走非法文件名后需重新 PIE 验证
> 来源：`D:\WeGameApps\rail_apps\OasisEraEditor(2001776)\ShadowTrackerExtra\Saved\Logs\ShadowTrackerExtra.log`、`ShadowTrackerExtra_TagLog.log`
> 更新时间：2026-09-04
> 关联主题：PIE调试非法字符校验失败, PIE调试与热更新边界
> 排除范围：本轮不是 Lua 语法错误，也不是 DS/客户端运行时崩溃；UGC 运行日志没有该弹窗文本
> 官方依据：`D:\知识库\和平精英绿洲起源\raw\docs\wiki\常见问题\20391_PIE调试失败原因与解决方案.md`

## 结论

本次编辑器调试在复制工程到上传目录之后被 CheckName 打断。弹窗原文是 `_tmp_lua_balance.py must begin with an alphabetic character.`，随后日志写成 `PIE 调试失败：上传文件名验证失败`。后续的 `lua file validation failed, debug interrupted` 是同一轮中断的跟随日志，不能当成 Lua 语法或逻辑错误。

UGC 客户端 FullLog `2026.09.04-10.42.08_client__dkck7sej3cjjhq_1.log` 只有准备阶段，没有该文件名；根因在编辑器主日志，不在项目运行日志。

## 关键证据

1. `2026.09.04-10.42.08`：`PIE debug: precondition check passed`，`PIE 调试：上传文件验证通过`。
2. `2026.09.04-10.42.32`：`PIE 调试：复制项目到上传目录成功，UploadPath=../../../ShadowTrackerExtra/Saved/UGCLinuxDebug/IslandAuctionKing`。
3. 同一秒弹窗：`LogUGCAlertDialog->Show sync dialog ... Content=_tmp_lua_balance.py must begin with an alphabetic character.`
4. `2026.09.04-10.42.38`：`CheckUploadFileNameFailed, Result: 10001`，`PIE 调试失败：上传文件名验证失败`，`PIE debug failed`。
5. `2026.09.04-10.42.50`：`PIE debug failed: lua file validation failed, debug interrupted`。

当时工程根目录存在 2026-09-03 留下的临时脚本：

- `_tmp_lua_balance.py`
- `_tmp_lua_balance2.py`
- `_tmp_patch_phase.py`
- `_tmp_widget_check.py`

处理：已移到 `Backup/TmpInvalidNames_20260904/`，并改名为字母开头的 `TmpLuaBalance.py`、`TmpLuaBalance2.py`、`TmpPatchPhase.py`、`TmpWidgetCheck.py`。Excel 锁文件已删除。根目录不再有 `_tmp_*.py`。

## 第二轮：pycache 点号与连字符

2026.09.04-11.02.47 再次弹窗：_mcp_tmp/__pycache__/patch_lua.cpython-312.pyc may contain the following characters: .- , please rename the file or path。随后 CheckUploadFileNameFailed, Result: 10001，PIE 调试失败：上传文件名验证失败。

工程内 .py/.pyc/.lua 扫描结果：只有这两个 .pyc 同时含 . 和 -。处理：已删除 _mcp_tmp/__pycache__，项目内不再有 .pyc 或 __pycache__。_mcp_tmp 下其余 .py 文件名为字母开头且不含点号/连字符，暂未移动。

## 残留风险

根目录仍有中文文件 海岛竞拍王_绿洲策划案.xlsx 和 全局状态同步档案.md。它们在 2026-09-01 的 PIE 会话前就已存在，不是本次弹窗点名对象。Screenshots、Tools/FlaUI.UiCapture 等目录含点号/连字符，但当前证据更像优先校验脚本类文件；若下一轮 CheckName 点名这些路径，再单独处理。

## 生效方式

重新调试 PIE。文件名校验发生在上传前，热更新无效。

## 2026-09-04 后续清理

已按全局提示词把 _mcp_tmp 和根目录一次性 .py 移到 D:\知识库\和平精英绿洲起源\备份\IslandAuctionKing\20260904_PromptAndTempCleanup，UGC 工程内不再保留这些临时脚本。
