# LLM Wiki Rules

## 目标
这个仓库用于维护一个可持续进化的知识系统。

## 目录约定
- `raw/docs/`：**全局通用原始资料**（官方 API、官方 Wiki、通用实战记录、工具链说明），按原样存储，只读，禁止改动。
- `raw/<项目名>/`：**地图项目专属资料**（数据参数、配置表快照、蓝图/UI 回读产物、资产清单、日志证据）。可写。
- `wiki/`：整理后的**通用**知识页。
- `wiki/项目/000_项目资料总览.md`：全局索引与项目专属资料之间的唯一接口。
- `备份/`：**项目调试备份**（写前 `.uasset` 副本、临时脚本、非法文件名隔离文件）。按 `备份/<ProjectName>/<YYYYMMDD>_<Theme>/` 存放。禁止把备份留在 UGC 工程内。本目录不进全局知识索引，也不作为 Wiki 正文。
- AGENTS.md（本文件）：仓库根目录下的工作规则。

## 创建文件存放边界（2026-09-14）

用户明确要求：区分“项目运行文件”和“非项目运行文件”。蓝图、Lua 脚本、导入到项目运行所需的图片/模型/音频等属于项目运行文件，应按项目流程存放；网页预览、Python 辅助脚本、Markdown/报告、截图及其他与地图运行无关的文件，**全部放在知识库内，禁止存放于 UGC 项目**。

1. 项目运行文件（如 `.uasset`、Lua、项目运行所需的导入资产）按项目目录、MCP 和 PIE 规则处理；网页预览、Python 辅助脚本、Markdown/报告、截图和其他非运行文件统一写入知识库。
2. Python 辅助脚本、临时网页和临时文档放在 `D:\知识库\和平精英绿洲起源\备份\<ProjectName>\<YYYYMMDD>_<Theme>\`；可复用网页、代码、模板和教程写入 `raw\知识\通用\<领域>\`，项目证据和回读结果写入 `raw\<ProjectName>\<子目录>\`。
3. 非运行文件不得复制、导入或残留在 UGC 工程；项目内已有文件可以只读检查，本条不授权删除、移动或覆盖项目现有文件。
4. 项目运行文件的蓝图/资源部署仍须遵守对应 MCP、备份、回读和 PIE 验证规则；知识库内的非运行文件仍须遵守 UTF-8、合法命名和临时文件清理规则。
5. 知识库内的每个非运行辅助文件都必须登记，保证后续可被关联问题检索、查阅和调用：通用文件登记到对应 `raw\知识\通用\<领域>\000_目录.md` 或关联 Raw 页，项目专属文件登记到 `raw\<项目名>\000_项目索引.md` 或 `raw\<项目名>\开发记录\000_辅助文件索引.md`。
6. 登记项必须包含：文件绝对路径、稳定文件名/编号、文件类型、关联主题/API/资产路径/错误文本、用途、入口或调用命令、输入输出、依赖、证据状态和更新时间。调用必须使用登记的绝对路径，不得依赖文件恰好位于 UGC 工程内。
7. Python、网页等可执行辅助文件必须提供可复现入口：Python 写明解释器和参数，网页写明直接打开方式或本地服务命令；文档和截图必须写明查阅入口及关联实体。调用前先读取登记项，调用后记录结果或失败原因。
8. **严禁在 UGC 工程内创建或残留名为 `$null` 的文件**（包括工程根、Asset、Saved、`_JsonOutput`、`Saved/UGCLinuxDebug/<项目名>/` 及其子目录）。该文件由 PowerShell `>$null` / `2>$null` 误重定向产生，`$` 开头会触发 `CheckUploadFileNameFailed Result 10001` 并中断 PIE。
9. 禁止在会落到 UGC 工程目录的命令里写 `>$null` / `2>$null`；丢弃输出统一用 `2>&1 | Out-Null`、`| Select-Object -Last N` 或 `-ErrorAction SilentlyContinue`。任务收尾必须递归扫描工程内 `$null` 并删除，复扫确认 0 命中。

## AI 配置归档（技能 / 插件 / MCP / 全局提示词，2026-09-16）

用户明确要求：对与《和平精英》绿洲起源项目编辑相关的**所有技能与 MCP 配置**做完整备份存档；**后续创建的技能、插件、MCP 一律存放于知识库**。

1. **归档根**：`D:\知识库\和平精英绿洲起源\工具\AI配置归档\`。总入口为 `000_归档索引.md`，新增资产契约见 `存放约定_新增技能插件MCP.md`。
2. **分层存放**：`skills\workbuddy\`、`skills\codex\`、`skills\shared_cc-switch\`、`skills\repo_oasis-skill-plus\` 四层镜像；`mcp\workbuddy\`、`mcp\codex\`；`plugins\workbuddy\`；`全局提示词\`；`归档记录\`。四个宿主目录之间**存在字节级版本漂移**，禁止合并成单份保存。
3. **先归档、后部署**：任何新技能 / 插件 / MCP 必须先在归档目录落权威副本 → 按固定字段登记到 `归档记录\<YYYYMMDD>_<主题>.md` → 补算 SHA256 到 `归档记录\校验清单_SHA256.txt` → 再复制到客户端运行位置。
4. **软件调用版全局提示词**：`…\AI配置归档\全局提示词\软件调用版_绿洲全局提示词.md`（`prompt_id: oasis.global.software`）。供任意客户端或脚本整文件注入；沿用原件措辞，客户端专属内容置于第 0.3 节「可选模块」，路径集中在第 2 节「运行环境上下文」。四份原件（WorkBuddy `oasis-global.md` / `island-auction-king-config.md` / `CODEBUDDY.md`、Codex `AGENTS.md`）同目录留档。
5. **冲突与漂移不得静默处理**：`oasis-prompt-optimize` 等技能在 WorkBuddy 与 Codex 两侧字节数不同；Codex 代理端口存在 `57321`（现网 `config.toml`）与 `8787`（`AGENTS.md` 要求）两说。均须在登记页保留双方来源与验证状态。
6. **排除项**：`__pycache__`、`*.pyc`、客户端 `plugins\cache` 内置插件正文、会话历史数据库不入档；`raw/docs/` 与 UGC 工程运行文件不得进入归档目录。

## 分层与索引隔离
1. **全局通用知识按原样存储**：`raw/docs/**` 与 `wiki/` 现有路径结构不变。`raw/docs/ai/*` 已被多个 wiki 页面按路径引用，不得搬迁或改名。
2. **地图项目专属数据落 `raw/<项目名>/`**：项目名必须匹配 `^[A-Za-z][A-Za-z0-9_]*$`；非法字符替换为 `_`，中文名取编辑器工程目录的英文名并另记中文别名，首字符为数字时前缀补 `P`。
3. 每个项目目录固定含 `000_项目索引.md` 与六个子目录：`开发记录/`、`数据参数/`、`配置表/`、`蓝图与UI/`、`资产清单/`、`日志证据/`。
4. **索引隔离**：项目专属资料不进 `wiki/000_索引.md`；只在项目自己的 `000_项目索引.md` 做局部索引，并在 `wiki/项目/000_项目资料总览.md` 加一行。全局检索默认只覆盖 `wiki/` 与 `raw/docs/`，项目层按需检索。
5. 完整规则与脚本见 [wiki/概念/知识库分层与索引隔离.md](./wiki/概念/知识库分层与索引隔离.md)。

## ingest 原则
当有新资料进入时：
1. **先分流**：判定是「全局通用知识」还是「地图项目专属数据」。出现项目路径（`/<项目名>/Asset/...`）、具体数值、资产名、DataTable 行值、PIE/DS 日志原文的，属项目专属。
2. 通用资料 → 生成来源页 → 提炼关键观点 → 更新相关 wiki 页 → 增加交叉链接 → 无对应页则新建，并登记进 `wiki/000_索引.md`。
3. 项目专属资料 → 确认 `raw/<项目名>/` 存在（不存在则按第 2、3 条建目录）→ 写入对应子目录 → 登记进该项目 `000_项目索引.md`。
4. **混合内容必须拆分**：通用部分入 `wiki/`，项目部分入 `raw/<项目名>/`，两侧互加链接。
5. 新项目首次入库时，在 `wiki/项目/000_项目资料总览.md` 的「已建项目」表加一行。

## 查询原则
1. 回答问题时，优先参考 `wiki/` 中已经整理过的页面；必要时再回看 `raw/docs/`。
2. 需要某个地图项目的具体数值、资产名或日志时，才检索 `raw/<项目名>/`，不要把项目层纳入默认检索范围。

## 图片素材生成（4k/gpt-image-2，2026-09-15 实测订正）

项目需要的 UI 背景、格子/图标、角色头像等高精度位图素材，统一使用 **4k/gpt-image-2** 模型生成。该模型**不是对话模型**，调用时必须遵守以下硬约束（完整流程、命令与错误码见 Raw 正文）：

1. **端点**：只走 `POST /v1/images/generations`；`/v1/chat/completions` 恒返回 502 `upstream_server_error`（2026-09-15 中/英文、长/短提示共 3 次全失败）。把该模型当对话模型在客户端里选用必然报错。
2. **尺寸**：实测上限 `2048x2048`，可用组合为 `1024x1024`、`1024x1536`、`2048x2048`、`auto`；`4096x4096`、`4096x2160`、字符串 `2K`/`4K` 返回 400，`1536x1024` 返回 503。名称里的 “4k” 不代表支持 4096。
3. **参数**：只接受 `model`、`prompt`、`n`、`size`；`quality`、`output_format`、`background` 会返回 400 `unsupported_parameter`。
4. **耗时与超时**：1024 约 56s、2048 约 346s，必须显式放大超时，否则被默认 120s 超时掐断。
5. **两条接入路径并存**：直连网关 `https://apihub.veletis.com/v1`（零脚本 curl，推荐）与 Codex 本地代理 `http://127.0.0.1:57321/v1`（OpenAI SDK）均为实测有效，不是互相否定的结论；任一路径失败先换另一条验证再判定为模型故障。
6. **产物与残留**：图片按项目交付路径保存；禁止在 UGC 工程内残留一次性 `.py`、脚本或缓存，优先使用零脚本方案。

- Raw 正文：[4k/gpt-image-2 图片生成流程](./raw/知识/通用/工具与流程/4k_gpt_image_2_图片生成流程.md)
- 实测来源：[2026-09-15 接口路由与尺寸实测](./raw/知识/通用/来源记录/2026-09-15_4k_gpt_image_2接口路由与尺寸实测.md)
- 索引：[wiki/概念/4k_gpt_image_2_图片生成流程](./wiki/概念/4k_gpt_image_2_图片生成流程.md)
- 生成后按 [资源导入与路径规范](./raw/知识/通用/配置与数据/资源导入与路径规范.md) 导入编辑器，并用 UGCAskQ MCP 写入回读。

## 当前入口
- Wiki 总索引：[wiki/000_索引.md](./wiki/000_索引.md)
- AI 配置归档（技能 / 插件 / MCP / 全局提示词）：[工具/AI配置归档/000_归档索引.md](./工具/AI配置归档/000_归档索引.md)
- 软件调用版全局提示词：[软件调用版_绿洲全局提示词.md](./工具/AI配置归档/全局提示词/软件调用版_绿洲全局提示词.md)
- 项目专属资料总览：[wiki/项目/000_项目资料总览.md](./wiki/项目/000_项目资料总览.md)
- 分层规则：[wiki/概念/知识库分层与索引隔离.md](./wiki/概念/知识库分层与索引隔离.md)
- 最近一次结构调整来源页：[wiki/来源/2026-08-26_知识库分层与索引隔离重构.md](./wiki/来源/2026-08-26_知识库分层与索引隔离重构.md)
- 最近一次入库来源页：[wiki/来源/2026-08-26_raw资料入库总结.md](./wiki/来源/2026-08-26_raw资料入库总结.md)
- 官方资料结构说明：[wiki/来源/官方API与Wiki文档结构.md](./wiki/来源/官方API与Wiki文档结构.md)

## 回写检查清单
每次回答后按顺序自检并在回复末尾汇报「知识库变动」：
1. 本轮结论已分流判定？通用的入 `wiki/`，项目专属的入 `raw/<项目名>/`。
2. 本轮结论是否已存在于 `wiki/`？不存在则新建或追加。
3. 新增通用页是否已登记到 `wiki/000_索引.md`，并与至少一个已有页互加相对链接？
4. 新增项目页是否已登记到该项目 `000_项目索引.md`，新项目是否已在 `wiki/项目/000_项目资料总览.md` 加行？项目专属页**不得**登记进全局索引。
5. 概念页是否符合固定结构：标题 → 来源与官方依据 → 核心结论 → 可执行步骤/代码 → 常见错误 → 相关页面 → 待查证？
6. 未被官方文档确认的内容是否单独列在「待查证」，而不是写成结论？
7. 所有相对链接与 `raw/` 引用路径是否真实存在？文件是否 UTF-8 无 BOM？
8. `raw/docs/` 为只读原始资料，禁止改动；`raw/<项目名>/` 可写。
9. 本轮新增或改动了技能 / 插件 / MCP / 全局提示词？是则须已按「AI 配置归档」第 3 条完成归档、登记与哈希，并在回复的「知识库变动」中列出归档绝对路径。

## Wiki 索引层约束（2026-08-26）
1. Wiki 只保留知识地图、模块目录、30 字以内摘要、Raw 相对链接与更新日志；详细正文、代码、教程、日志、图片均写入 Raw。
2. 通用详细内容写入 `raw/知识/通用/<模块>/`；项目详细内容写入 `raw/<项目名>/<子目录>/`。
3. 新建或更新 Raw 文件后，必须在对应 Wiki 索引页增加一条链接和更新摘要；不得把 Raw 正文复制进 Wiki。
4. 非运行辅助文件的索引、查阅和调用规范见 [非运行辅助文件索引与调用规范](raw/知识/通用/知识库治理/非运行辅助文件索引与调用规范.md)。
5. 推荐目录、Wiki 模板和完整示例见 `raw/知识/通用/知识库治理/`。
