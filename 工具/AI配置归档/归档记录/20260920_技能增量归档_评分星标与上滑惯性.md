# 2026-09-20 技能增量归档（评分星标污染 / 上滑惯性）

> 类型：知识库工程资产 / 归档记录
> 主题：`oasis-ocr-sampler-calibrate` 技能第四次更新入档（同日）
> 适用范围：本页仅登记 2026-09-20 该技能的本次增量；首次入档与同日前三次更新见 `20260920_技能增量归档_ADB上滑.md`，全量快照见 `20260916_技能与MCP全量归档.md`
> 证据状态：项目实测（本机归档操作 + 逐文件 SHA256；技能内容依据为真机采样复现与自检/回归结果）
> 来源：2026-09-20 会话。用户真机采样反馈「评分出现 `74.3`/`?2.9`」「上滑滑过头」，修复过程中更新了该技能，按 [存放约定_新增技能插件MCP.md](../存放约定_新增技能插件MCP.md)「先归档、后部署」补档
> 更新时间：2026-09-20
> 关联主题：`../000_归档索引.md`、`../../../raw/知识/通用/工具与流程/绿洲启元地图数据OCR采样工具.md`
> 排除范围：客户端缓存目录（`plugins\cache`、`*.db`）；UGC 工程运行文件
> 官方依据：官方未覆盖；本条为仓库工程约定

---

## 背景

`oasis-ocr-sampler-calibrate` 于 2026-09-20 当日第四次更新：新增「评分误读排查」章节、把「评分框左边界固定 112」的**旧铁律改写为作废**（改为 126@1920 ≡ 视图 71.0）、新增评分形态匹配铁律与连续滚动上滑手势铁律、判读基线加入 **G / H 两段防回归**与「假值个数」判据、环境坑补入「自检约 1 分钟别读早」「`*>` 独占句柄」「safe-delete 可能真失败」。按「先归档、后部署」同步归档层。

## 登记项

### AICFG-20260920-001（更新·第四次） oasis-ocr-sampler-calibrate

- 资产类型：技能
- 归档绝对路径：`D:\知识库\和平精英绿洲起源\工具\AI配置归档\skills\workbuddy\oasis-ocr-sampler-calibrate\SKILL.md`
- 来源绝对路径：`C:\Users\Administrator\.workbuddy\skills\oasis-ocr-sampler-calibrate\SKILL.md`
- 部署目标绝对路径：`C:\Users\Administrator\.workbuddy\skills\oasis-ocr-sampler-calibrate\`
- 本次新增/改写的关联实体：
  - 函数 / 常量：`parse_score()` / `SCORE_RE` / `FIELD_PARSERS` / `swipe_args()` / `SWIPE_PCT_DEFAULT=0.35` / `SWIPE_MS_DEFAULT=800`；几何 `GEOM_VERSION` **v3 → v4**；`LOBBY["fields"]["评分"]` `(62.9,…) → (71.0,-5.0,115.0,26.0)`
  - 自检新增段：**G 段**（评分解析 14 单元用例 + 真机 6 卡端到端）、**H 段**（上滑参数断言，按 `"swipe"` 关键字定位取参）、**I 段**（滑动位移测量：两类合成图各 5 档已知位移 + 无关两帧返回 `None` + `swipe_advice` 五分支）
  - 新增触发词：「评分读成 74.3/?2.9」「评分出现 0-10 之外的数」「上滑滑过头/一次滑过一页多」「**测试滑动没反应/看不到滑了多少**」「怎么调上滑幅度」
  - 函数：`measure_scroll()`（多块模板匹配 + 中位数投票，测不出返回 `None`）、`swipe_advice()`（判 没滑够/合适/滑过头 + 反推建议幅度）；常量 `MANUAL_MEASURE_WAIT=5.0`、`SWIPE_SETTLE_SEC=1.0`
  - 报错 / 现象实体：`74.3`、`?2.9`、`食24`、`A2→2`、`INJECT_EVENTS`、`InputShellCommand.sendSwipe`
- 用途：标定/修正绿洲启元地图数据 OCR 采样器（大厅列表页 2 行 × 3 列识别）与 ADB 采样通路，并用真实截图 / 真机回归验证
- 调用入口：技能由 WorkBuddy 自动按触发词加载；工具本体与两个验证入口同 `20260920_技能增量归档_ADB上滑.md` 所载
- 输入 / 输出：输入=真机截图或拍照导入 + 手拖字段框；输出=`_selftest.txt` / `_regress.txt` / `crops\regress_boxes.png` / `out\*.csv|json`；技能本身无返回值
- 依赖：Python 3.12.10（工具专属 `.venv`）；`adb.exe`（`D:\知识库\和平精英绿洲起源\工具\platform-tools\adb.exe`，未加 PATH）
- 版本：未标注（SKILL.md frontmatter 无 `metadata.version`）
- 证据状态：项目实测（2026-09-20 自检 A/A2/B/C/D/E/F/**G**/**H** 全通过；回归 **26/30**、卡片定位 6/6、评分列由「2/6 + 1 假值」变为「3/6 + 0 假值」）
- 生命周期：可复用；清理条件：工具 `OasisOcrSampler` 停用或技能被合并
- 更新时间：2026-09-20

## SHA256

```
FD8FB339ED4C54E83150AEA7F17A27DB7EE3C5390C1DF37C1F9548B1A3AD3039  skills\workbuddy\oasis-ocr-sampler-calibrate\SKILL.md  28152
```

> 同一天六次更新的体积序列：15,007 B（`FB12DB…`）→ 17,687 B（`9D856A…`）→ 17,728 B（`93669B…`）→ 23,794 B（`2704B3…`）→ 26,351 B（`824344…`）→ **28,152 B（`FD8FB3…D3039`，本页为准）**。
> 注一：第 4 版入档后改过一字之差的一处措辞 —— 「不要为了『救回它』在解析层补小数点」属**动机条件式禁止**（把禁令挂到自证目的上，留下「我目的不是救回它」的开脱钩子），已改写为锚定行为本身的「**解析层不补小数点（无条件）**」。
> 注二（第 5 次更新）：用户反馈「测试滑动不会实际滑动，不方便测试滑动幅度」→ 新增铁律 15（凡需现场标定的自检必须输出量化结果）、「幅度怎么标定」步骤、`measure_scroll` / `swipe_advice` 的 `None` 优先原则、I 段说明与触发词。
> 注三（第 6 次更新）：用户反馈「结果无法导出为表格」→ 新增铁律 16（按钮回调异常必须接住，否则表现为「点了没反应」）与 17（写文件抽成模块级纯函数，别写在回调里；**类体引用的常量必须定义在 `class App` 之前**）、J 段说明、触发词「导出失败 / 导出 Excel / 找不到导出文件」、环境要点补 openpyxl 3.1.5 及安装命令。

（已同步至 `校验清单_SHA256.txt`：刷新本技能条目 + 新增本记录文件条目。）

## 同步动作清单

- [x] 技能包同步到归档层 `skills\workbuddy\oasis-ocr-sampler-calibrate\SKILL.md`
- [x] `校验清单_SHA256.txt`（刷新技能条目 + 新增本页条目）
- [x] 本页登记项
- [x] 归档内无 `__pycache__` / `*.pyc` / 临时文件（该技能包仅 1 个 `SKILL.md`）
- [ ] **未同步至 Codex 侧（`~/.codex/skills`）与 cc-switch 共享源** —— 与 `20260920_技能增量归档_ADB上滑.md` 同一遗留项，见 `../000_归档索引.md` 第十节第 5 条

## 相关页面

- [000_归档索引.md](../000_归档索引.md)
- [存放约定_新增技能插件MCP.md](../存放约定_新增技能插件MCP.md)
- [20260920_技能增量归档_ADB上滑.md](./20260920_技能增量归档_ADB上滑.md)（首次入档与同日前三次更新）
- [20260916_技能与MCP全量归档.md](./20260916_技能与MCP全量归档.md)
- [绿洲启元地图数据OCR采样工具](../../../raw/知识/通用/工具与流程/绿洲启元地图数据OCR采样工具.md)
- [2026-09-20_真机采样复现_评分星标污染与上滑惯性](../../../raw/知识/通用/来源记录/2026-09-20_真机采样复现_评分星标污染与上滑惯性.md)
