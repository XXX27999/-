# 2026-08-28 新手教程纽莱宠物奖励与护理动画

> 类型：项目开发记录
> 主题：新手教程完成奖励、纽莱宠物生成、喂食/清洗护理动画
> 适用范围：项目 `pet_paradise`；仅适用于当前项目脚本与资源状态
> 证据状态：已根据 DebugID 日志确认服务端生成纽莱成功、护理动画播放成功、移动状态进入成功；已确认纽莱游戏蓝图误用 AnimationSingleNode 导致移动 BlendSpace 不生效，并通过 UGCAskQ 修正为 AnimationBlueprint；已完成 PetData 1053 与 NLCWD 行 1 的 MCP 写后回读；本轮 PIE 启动被项目内既有带连字符备份文件名校验阻断，且用户明确不需要 PIE
> 来源：项目 Lua、UGCAskQ 只读查询、官方 API 文档
> 更新时间：2026-08-28
> 关联主题：新手教程、宠物存档、宠物护理动画、RPC 分发
> 排除范围：未修改蓝图结构节点或 `_JsonOutput`；本轮仅通过 MCP 修改 PetData 1053 的头像路径，未把 1053 加入任何宠物池；未证明本轮 DataTable 修复已在 PIE 运行时展示

## 已确认

1. `Script/Blueprint/CF_KHDRPC.lua` 的 `ServerRPC_ClaimTutorialReward(self, claimReason)` 是新手教程完成/跳过领奖入口。`complete` 与 `skip` 分支都调用 `Script/Function/NewPlayerTutorialPetReward.lua`，服务端通过 `UGCPlayerState:SpawnPet` 生成固定路径宠物，再发放金币。
2. 固定宠物路径为：
   `Asset/Blueprint/Prefabs/Monsters/nuilai/nuilai1.nuilai1_C`
3. `Script/Blueprint/Prefabs/Monsters/Pet.lua` 使用 `PetPath` 映射护理动画键。本轮新增 `nuilai1_C`：
   - Feed：`animals/niulai/Eat.Eat`
   - Clean：`animals/niulai/Clean.Clean`
4. `Script/Blueprint/CF_FWDRPC.lua` 会将 `PetPath` 传到客户端，客户端调用 `Pet:PlayCareAnimation`；因此新映射可进入现有 `UGCGameSystem.GetUGCResourcesFullPath` 加载与 `USkeletalMeshComponent:PlayAnimation` 播放流程。
5. `Script/Function/NewPlayerTutorialPetReward.lua` 按 `PetDataStrct.Path` 去重，生成成功后请求已有 `ClientOpenChongWuChouQu` 开蛋帧动画，并传入展示名 `nuilai1`。工作区已存在 `Asset/Pet3D/nuilai1.uasset` 与 `Asset/Pet3D/nuilai12D.uasset`，可以进入现有宠物 2D/3D 展示流程。

## 旧日志复核与本轮修正

1. 项目旧 DebugID `dkck7s8e3qgk588` 的 DS 日志 `D:/WeGameApps/rail_apps/OasisEraEditor(2001776)/ShadowTrackerExtra/Saved/Logs/pet_paradise/DSlog/FullLog/2026.08.28-18.03.25_ds__dkck7s8e3qg588_realtime.log` 第 11731-11740 行确认：`ServerRPC_ClaimTutorialReward` 收到 `claimReason=skip`，且 `HasAuthority=true`、`eligible=true`、`granted=false`，随后执行“仅发放金币，不发放完成宠物”，奖励提示为“金币 +10000”。
2. 同一 DebugID 的客户端日志 `D:/WeGameApps/rail_apps/OasisEraEditor(2001776)/ShadowTrackerExtra/Saved/Logs/pet_paradise/Clientlog/TagLog/2026.08.28-18.00.29_client__dkck7s8e3qg588_1_TagLog.log` 第 970-971 行确认：跳过按钮已成功发出 `claimReason=skip` 请求；第 1006-1008 行确认百科 `MainHelpUI` 以 `zOrder=1000` 打开。日志没有出现本次新奖励模块的 `Grant`/`SpawnPet` 成功链路，因为旧代码的 skip 分支没有调用它。
3. `Script/Blueprint/CF_KHDRPC.lua` 现已让 `complete` 与 `skip` 共用 `NewPlayerTutorialPetReward.Grant`，并统一提示金币与宠物纽莱；`Script/Blueprint/CF_FWDRPC.lua` 的孵化抽取 UI 层级改为 `1200`，`Script/Blueprint/Prefabs/UI/ShanDian/QueRen/ChongWuChouQu.lua` 的孵化结果 UI 同样改为 `1200`，覆盖百科说明的 `1000` 层级。

## 查证结果

- 工作区文件已确认：`animals/niulai/Eat.uasset`、`animals/niulai/Clean.uasset`、`Asset/Pet3D/nuilai1.uasset`、`Asset/Pet3D/nuilai12D.uasset`。
- UGCAskQ `ue_py` 只读加载已确认 `/pet_paradise/animals/niulai/Eat.Eat` 与 `/pet_paradise/animals/niulai/Clean.Clean` 均为 `AnimSequence` 且加载成功；`schema` 查询不适用于直接以动画对象路径查询字段，不能据此判定资源不存在。
- UGCAskQ schema 已确认 `/pet_paradise/Asset/Pet3D/nuilai1.nuilai1_C` 为可用展示蓝图，`nuilai12D` 的 schema 查询未命中，但对应磁盘文件已存在，运行时加载仍需 PIE 验证。

## 最新日志复核与修正

1. 最新 DebugID `dkck7s8e3qgk6z` 的 DS TagLog `D:/WeGameApps/rail_apps/OasisEraEditor(2001776)/ShadowTrackerExtra/Saved/Logs/pet_paradise/DSlog/TagLog/2026.08.28-18.09.21_ds__dkck7s8e3qgk6z_taglog.log` 第 1903-1952 行确认：`claimReason=complete`、`HasAuthority=true`，`NewPlayerTutorialPetReward+Grant` 返回 `granted=true`，纽莱 Actor 已生成，奖励流程成功；但第 1945-1946 行 `modelName=` 为空。
2. 同一 DebugID 的客户端 FullLog `D:/WeGameApps/rail_apps/OasisEraEditor(2001776)/ShadowTrackerExtra/Saved/Logs/pet_paradise/Clientlog/FullLog/2026.08.28-18.06.47_client__dkck7s8e3qgk6z_1.log` 第 140586-140589 行确认：客户端收到开蛋 RPC 并打开 UI，但因 `modelName` 为空走“仅播放开蛋帧动画”分支；因此“有孵化动画、无宠物展示”是展示参数为空导致，不是宠物生成失败。
3. `Script/Blueprint/CF_FWDRPC.lua` 的 `ClientOpenChongWuChouQu` 现增加客户端兜底：当 `petName=纽莱` 且 `modelName` 为空时，使用 `displayModelName="nuilai1"`，再调用 `ChongWuFuHua:SetModelName`；同时记录原始参数、兜底参数和 `HasAuthority`，便于下一次日志确认。
4. UGCAskQ `ue_py` 只读确认 `/pet_paradise/Asset/Pet3D/nuilai12D.nuilai12D` 为 `TextureRenderTarget2D`，`/pet_paradise/Asset/Pet3D/nuilai1.nuilai1_C` 为 `BlueprintGeneratedClass`，展示链路所需资源类型正确。

## 最新移动动画问题复核与修正

1. 最新 DebugID `dkck7s8e3qhsw6` 的 DS TagLog `D:/WeGameApps/rail_apps/OasisEraEditor(2001776)/ShadowTrackerExtra/Saved/Logs/pet_paradise/DSlog/TagLog/2026.08.28-18.36.16_ds__dkck7s8e3qhsw6_taglog.log` 第 1718-1720、2127-2129 行确认：`nuilai1_C_0` 实际进入 `PawnState.Movement.Walking`；因此不是跟随移动或行为树未触发。
2. UGCAskQ 只读对比 `/pet_paradise/Asset/Blueprint/Prefabs/Monsters/horse/horseA.horseA_C`、`sheepA.sheepA_C` 与 `nuilai1.nuilai1_C`：旧宠物 `Mesh.AnimationMode=0`（`AnimationBlueprint`），纽莱为 `1`（`AnimationSingleNode`）。纽莱 `Mesh.AnimationData.AnimToPlay=None`，单节点没有可播放的移动资产。
3. UGCAskQ 只读确认纽莱 `AnimListComp.GameplayTagAnimDataListDefault` 的两个移动条目均有效：`GenericCharacterAnim.General.Move.Relax` 与 `GenericCharacterAnim.General.Move.Battle` 均指向 `/pet_paradise/animals/niulai/NewBlendSpace1D.NewBlendSpace1D`。该资源为有效 `BlendSpace1D`，含 3 个有效样本，并使用纽莱 `niulai_2__Skeleton`。
4. 已备份 `D:/WeGameApps/rail_apps/OasisEraEditor(2001776)/ShadowTrackerExtra/UGCProjects/pet_paradise/Asset/Blueprint/Prefabs/Monsters/nuilai/nuilai1.uasset` 至 `D:/WeGameApps/rail_apps/OasisEraEditor(2001776)/ShadowTrackerExtra/UGCProjects/pet_paradise/Saved/Backup/2026-08-28_nuilai1_before_animation_mode_fix.uasset`，通过 UGCAskQ PRV 将 `Mesh.AnimationMode` 改为 `0` 并保存。
5. UGCAskQ 写后回读确认：`AnimationMode=0`、`AnimClass=ABP_UGC_MobMainAnimInstance_C`，两条移动 BlendSpace 仍为有效资源。尚未 PIE，故“移动动画已在实际运行中播放”仍需重新 PIE 验证。

## 次要日志异常

- 同一 DS 日志存在 `CharMoveComp` 不存在 `SetVelocity` 的属性错误；它发生在护理状态切换相关逻辑中，当前证据不足以证明它是移动动画不播放的根因，暂不扩大修改范围。
- 旧会话客户端的 `AnimSingleNodeInstance.GetClass` 报错来自 `Pet.lua` 的诊断读取，不是资源加载失败；修正动画模式后需在新 PIE 日志中确认该异常是否消失。

## 挖矿动画复核

1. 挖矿不走 `Eat/Clean` 护理资源。项目调用链是 `Pet:StartWork` 设置工作状态并发送 `RPC_StartWork`，客户端 `Pet:RPC_StartWork` 创建循环定时器，随后调用 `Pet:PlayWorkAnimation`。
2. `Pet:PlayWorkAnimation` 对没有旧版攻击序列映射的纽莱使用客户端 `UGCGenericCharacterSystem.PlayAnimMontageByTag`，Tag 为 `GenericCharacterAnim.ExtendAnim`。该 API 的官方文档标明生效范围为客户端。
3. UGCAskQ 只读确认纽莱 `AnimListComp.GameplayTagAnimDataListExtend` 的 `GenericCharacterAnim.ExtendAnim` 已挂载 `/pet_paradise/animals/niulai/NewAnimMontage.NewAnimMontage`，资源为 `AnimMontage`，使用 `niulai_2__Skeleton`，长度约 2.002 秒，含 `DefaultSlot` 和 1 个动画段。
4. 因此挖矿动画没有显示与移动动画使用同一根因：旧运行时 `Mesh.AnimationMode=1` 会绕过通用动画蓝图和 AnimListComp 的 Montage 播放；该模式已改为 `AnimationBlueprint=0`。尚未重新 PIE，不能把配置回读升级为“运行时已播放”的结论。

## 依据文件

- 项目奖励入口：`Script/Blueprint/CF_KHDRPC.lua`
- 项目奖励模块：`Script/Function/NewPlayerTutorialPetReward.lua`
- 宠物护理映射：`Script/Blueprint/Prefabs/Monsters/Pet.lua`
- 客户端护理调用：`Script/Blueprint/CF_FWDRPC.lua`
- 旧 DebugID 日志：`D:/WeGameApps/rail_apps/OasisEraEditor(2001776)/ShadowTrackerExtra/Saved/Logs/pet_paradise/DSlog/FullLog/2026.08.28-18.03.25_ds__dkck7s8e3qgk588_realtime.log`、`D:/WeGameApps/rail_apps/OasisEraEditor(2001776)/ShadowTrackerExtra/Saved/Logs/pet_paradise/Clientlog/TagLog/2026.08.28-18.00.29_client__dkck7s8e3qgk588_1_TagLog.log`
- 最新 DebugID 日志：`D:/WeGameApps/rail_apps/OasisEraEditor(2001776)/ShadowTrackerExtra/Saved/Logs/pet_paradise/DSlog/TagLog/2026.08.28-18.09.21_ds__dkck7s8e3qgk6z_taglog.log`、`D:/WeGameApps/rail_apps/OasisEraEditor(2001776)/ShadowTrackerExtra/Saved/Logs/pet_paradise/Clientlog/FullLog/2026.08.28-18.06.47_client__dkck7s8e3qgk6z_1.log`
- 官方资源路径：`D:/oasis-skill-plus/docs/api/class/和平全局接口/基础功能/UGCGameSystem.md`
- 官方骨骼动画：`D:/oasis-skill-plus/docs/api/class/Others/USkeletalMeshComponent.md`
- 官方动画模式枚举：`D:/oasis-skill-plus/docs/api/cppenum/E/EA/EAnimationMode.md`
- 官方 BlendSpace：`D:/oasis-skill-plus/docs/api/class/Others/UBlendSpace1D.md`
- UGCAskQ 只读资产验证：`/pet_paradise/animals/niulai/Eat.Eat`、`/pet_paradise/animals/niulai/Clean.Clean`
- UGCAskQ 读写回读资产：`/pet_paradise/Asset/Blueprint/Prefabs/Monsters/nuilai/nuilai1`

## PetData 1053 名称与头像未生效排查

### 项目实测

1. UGCAskQ 只读回读 `/pet_paradise/Asset/Data/Table/Customized/PetData.PetData` 确认共有 23 行；1053 行实际值为：`ItemID=1053`、`Name=牛来`、`PetPath=Asset/Blueprint/Prefabs/Monsters/nuilai/nuilai1.nuilai1_C`、`IconPath=Image/Pet/niulai.niula`。头像路径少了末尾一个 `i`，而 `/pet_paradise/Image/Pet/niulai.niulai` 可加载为 `Texture2D`。
2. 已备份当前表格快照至项目外 `D:/OasisBackups/pet_paradise/PetData_20260828_after_icon_fix.uasset`，并通过 UGCAskQ `Resolve -> Plan -> Execute` 将 1053 的 `IconPath` 修正为 `Image/Pet/niulai.niulai`；写后独立回读确认名称、宠物路径、头像路径均正确。
3. 当前生效购买链为 `Script/Blueprint/UGCPlayerController.lua:701` 的 `OnBuyPet` 和 `Script/Blueprint/CF_KHDRPC.lua:1353` 的购买结果监听：绿洲币宠物按 `Shop` 行名映射到 `PetPool`；金币宠物由 `CF_KHDRPC.ServerRPC_BuyPetFromPool` 读取 `CoinShop` 与 `PetPool`。这两条当前链路均不会读取 `PetData`。
4. `Script/gamemode/PlayerGetMSCard.lua:491` 虽然保留了读取 `PetData` 的旧 `OnBuyPet`，但 `Execute` 中的 Delegate 注册已注释，且旧 `PetPathMap` 仅覆盖到 1052。因此仅新增 PetData 1053 行不会让当前商城购买链生成“牛来”。

### 官方依据与生效边界

- `D:/oasis-skill-plus/docs/api/class/和平全局接口/基础功能/UGCGameSystem.md`：`UGCGameSystem.GetTableDataByRowName` 支持运行时按表路径和行名读取 DataTable，客户端/服务端均可调用。
- 本次表格修改属于编辑器资产修改，按项目规范需重新 PIE 才能验证运行时；用户本轮明确不需要 PIE，因此当前仅确认编辑器资产持久化和 MCP 回读成功。
- PIE 尝试被项目现有 `Saved/Backup/2026-08-28_nuilai1_before_animation_mode_fix.uasset` 的连字符文件名校验阻断，日志记录为 `PIE 调试失败：上传文件名验证失败`；该文件不是本轮新增，未删除或改名。

### 结论

- **已修复**：PetData 1053 头像路径拼写错误。
- **仍需配置决策**：若“牛来”应从宠物蛋抽取，必须明确加入 `DJCWD`、`ZJCWD`、`GJCWD` 或其他目标 `PetPool`；仅修改 `PetData` 不会加入当前抽取池。本轮未猜测宠物蛋等级，也未修改宠物池。

## NLCWD 头像未加载复核与修正

### 项目实测

1. 最新会话 DebugID 为 `dkck7s8e3qisyd`。定向日志分析未找到 `NLCWD`、`5005`、`IconPath`、`TexturePath` 或 `ServerRPC_BuyPetFromPool` 的完整命中，结论是“缺少调用证据”，不能据此断言 DataTable 加载失败。DS 日志另有 `/pet_paradise/animals/niulai/` 下 `NewMaterialInstanceConstant`、`Idle`、`NewAnimMontage`、`ZL1/ZL2/ZL3` 依赖缺失，属于新宠物蓝图运行时依赖问题，与头像路径为空是两个独立问题。
2. UGCAskQ 独立回读 `/pet_paradise/Asset/Data/Table/Customized/PetPool/NLCWD.NLCWD` 行 `1` 确认：`Id=5005`、`ModelName=nuilai1`、`Name=牛来`、`PetPath=Asset/Blueprint/Prefabs/Monsters/nuilai/nuilai1.nuilai1_C`、`Texture=Image/Pet/niulai.niulai`；`/pet_paradise/Image/Pet/niulai.niulai` 可加载为 `Texture2D`。
3. 写入前该行 `PetPath` 是字面量 `UGCGameSystem.GetUGCResourcesFullPath('Asset/...')`。`UGCPlayerState:SpawnPet` 内部还会再次调用 `GetUGCResourcesFullPath(Path)`，因此该字面量不是可传入的资源相对路径。已通过 UGCAskQ `Resolve -> Plan -> Execute` 修正并保存，写后回读为相对路径；备份为 `D:/OasisBackups/pet_paradise/NLCWD_20260828_before_path_fix.uasset`。
4. 修正前 `Script/Function/NewPlayerTutorialPetReward.lua` 没有读取 `NLCWD`，并在 `SpawnPet` 第五个参数传入空字符串。`UGCPlayerState:SpawnPet` 会把该参数写入 `WarehouseData.TexturePath`，所以后续 UI 没有头像路径可加载，这是当前“宠物已生成但头像未加载”的直接根因。

### 本轮代码修正

1. `Script/Function/NewPlayerTutorialPetReward.lua` 新增 `GetTutorialPetConfig`，在 `Grant` 运行时用 `UGCGameSystem.GetTableDataByRowName` 读取 `Asset/Data/Table/Customized/PetPool/NLCWD.NLCWD` 行 `1`，读取 `PetPath`、`Name`、`Texture`、`ModelName`，并保留默认值、字段校验、入口/错误/出口日志。
2. `Grant` 将表格头像路径传给 `SpawnPet`，`NotifyHatchAnimation` 也改为使用表格名称和模型名；不再固定传空头像。
3. 已使用编辑器自带 LuaCheck 检查 `Script/Function`，结果为 `All lua file num=1, fileErrNum=0, allErrNum=0`。

### 生效边界

- NLCWD 与初始化奖励链路属于 DataTable/运行时初始化变更，按官方 `UGCGameSystem.GetTableDataByRowName` 规则和项目规范需要重新 PIE 才能做运行时验证；用户本轮明确“不需要 PIE”，因此本记录只确认编辑器资产回读与静态检查通过，不宣称运行时已经显示头像。
- 若目标是让 `NLCWD` 参与商城宠物蛋抽取，当前 `CF_KHDRPC.ServerRPC_BuyPetFromPool` 的白名单和商城/宠物池映射仍未包含 `NLCWD`；本轮未猜测商品价格、蛋等级或入口，未改动该购买链。
