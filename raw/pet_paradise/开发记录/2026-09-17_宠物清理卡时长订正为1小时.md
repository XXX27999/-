# 2026-09-17 宠物清理卡（8310014）时长 24 小时 → 1 小时订正

> 类型：配置订正 / 数值口径对齐
> 主题：UGCBattleItem 8310014「宠物清理卡」时长与全部详情文本由 24 小时订正为 1 小时
> 适用范围：pet_paradise（宠物乐园）工程，涉 `UGCBattleItem` / `UGCObject` / `CoinShop` 三张表 + `HelpLienCard` 蓝图 CDO + 3 个 Lua
> 证据状态：项目实测（MCP 写入前后 before/after 回读 + 同步后 JSON 复核 + luaparser AST 校验）
> 来源：本次对话实测；《宠物乐园(商业化)测试用例》xmind 商业化口径；`20260917_商业化测试用例\一致性审计报告.md` 偏差项 B4
> 更新时间：2026-09-17
> 关联主题：限时卡存档（`WorkTimedCardHelper`）、DataTable 行文本一致性、商业化测试用例一致项
> 排除范围：不动 `CARD_DURATION_SEC`（打工收益卡的 24 小时，属另一道具）；不动存档 key（`ShitAutoCleanCardEndTime`）
> 官方依据：`UGCAskQ` MCP `data_table_modify_row` / `get_cdo` / `set_property` / `save_package`（`ue_read py:guide datatable`、`py:guide property`、`py:workflow blueprint`）

---

## 一、订正依据

商业化测试用例口径：宠物清理卡 **1 小时**。原工程实现为 24 小时，被审计列为确认偏差 **B4**（代码与物品表自洽，但均偏离用例）。本次按用例订正，并使**全部展示文本**同步。

## 二、改动清单（7 处，含 4 处资产 + 3 个 Lua）

### 资产侧（MCP 写入，均 save_package 并重新加载回读验证）

| # | 资产 | 行 / 对象 | 字段 | 改前 | 改后 |
| --- | --- | --- | --- | --- | --- |
| 1 | `/pet_paradise/Asset/Data/Table/UGCBattleItem` | 行 `8310014` | `ItemDetails` | 24 小时内宠物不会降低清洁度 | **1 小时内宠物不会降低清洁度** |
| 2 | `/pet_paradise/Asset/Data/Table/UGCObject` | 行 `1011` | `ItemDesc` | 24 小时内宠物不会降低清洁度 | **1 小时内宠物不会降低清洁度** |
| 3 | `/pet_paradise/Asset/Data/Table/Customized/CoinShop` | 行 `CWQLK` | `Introduction_14_B8270B5E4D1B5DC4B6E919AC6E78D3B5` | 24小时内不会降低清洁度 | **1小时内不会降低清洁度** |
| 4 | `/pet_paradise/Asset/Blueprint/Prefabs/Items/WorkItems/WorkRewardItem/HelpLienCard` | CDO（`ItemID=8310014`） | `ItemDetail` | 24 小时内宠物不会降低清洁度 | **1 小时内宠物不会降低清洁度** |

- 第 3 项沿用原表无空格写法（`1小时内`），保持最小差异，避免无谓改动。
- 第 4 项探测 `ItemDetail` 无 EditCondition 门控（`get_uproperty().has_metadata('EditCondition') == False`），直接写入即生效。

### Lua 侧（热更可生效）

| 文件 | 行 | 改动 |
| --- | --- | --- |
| `Script/Common/BatchUseItemConfig.lua` | 8-11 | `ShitAutoCleanSecondsPerItem` 86400 → **3600**；注释与口径说明同步 |
| `Script/Common/Utils/WorkTimedCardHelper.lua` | 10-11、18-21 | 新增 `SHIT_AUTO_CLEAN_DEFAULT_DURATION_SEC = 3600`；`PET_CLEAN_CARD_ACTIVE_TIP` 文案改「1 小时内」；注释同步 |
| `Script/Blueprint/Prefabs/Items/WorkItems/WorkRewardItem/HelpLienCard.lua` | 102、106-107 | 注释同步；**兜底常量由 `CARD_DURATION_SEC`（打工收益卡 24h）改为 `SHIT_AUTO_CLEAN_DEFAULT_DURATION_SEC`（1h）** |

### 附带修复（口径隐患）

`HelpLienCard.lua:106` 原兜底取 `WorkTimedCardHelper.CARD_DURATION_SEC`（**打工收益卡的 24 小时**）。若 `BatchUseItemConfig.GetConfig` 因改名/缺失返回 nil，清理卡会按 24 小时续期，与 1 小时口径冲突。已改为清理卡专用兜底常量。

保留未动：`WorkTimedCardHelper.CARD_DURATION_SEC = 24 * 60 * 60` 是打工收益卡自身时长，正确，不得改。

## 三、验证

1. **MCP 写入回读**：三张表 `mod_ok=True` / `saved=True`，`before` → `after` 文本对比一致；CDO `before=24 小时内…` → `after=1 小时内…`，`ItemID/ItemName` 未变（8310014 / 宠物清理卡）。
2. **JSON 同步复核**：`UAssetToJsonConverter --mode convert` 全量 1781/1781 成功 0 失败；同步后 4 处 JSON `Value` 均为「1 小时…」。
3. **Lua AST**：3 个文件 `luaparser` 解析全部通过。
4. **残留扫描**：`_JsonOutput/Asset` 内再无「24 小时 + 清洁度」组合；`ExtendResource/`、`Script/UI/` 内无「清理卡」「24 小时」残留。

## 四、生效方式

- Lua 三处为普通函数体/常量，**可热更新**。
- 资产四处（DataTable + 蓝图 CDO）已 `save_package` 落盘，**建议重新调试 PIE** 验证实际生效（尤其确认背包内已存在旧卡叠加时的续期表现）。

## 五、备份

`D:\知识库\和平精英绿洲起源\备份\pet_paradise\20260917_petcleancard_1h\`（4 个 `.uasset` + 3 个 `.lua` 原件 + 辅助脚本）。
