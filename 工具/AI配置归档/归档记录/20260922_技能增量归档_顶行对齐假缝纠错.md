# 2026-09-22 技能增量归档：顶行对齐「假缝」误判纠错

> 类型：归档记录
> 主题：`oasis-ocr-sampler-calibrate` 第 12 次更新登记（顶行对齐「假缝」误判纠错 / 删除被证伪的 `scrolled_past`）
> 证据状态：项目实测
> 来源：2026-09-22 会话。用户反馈「有时候滑动到红线识别也不准，地图顶部都滑上去超出屏幕了，还报顶行已对齐红线。按照滑动到图片中每张地图位于红框情况。」
> 更新时间：2026-09-22
> 关联主题：[000_归档索引.md](../000_归档索引.md)、[绿洲启元地图数据OCR采样工具](../../../raw/知识/通用/工具与流程/绿洲启元地图数据OCR采样工具.md)、[2026-09-22_对齐误判假缝与首行完整](../../../raw/知识/通用/来源记录/2026-09-22_对齐误判假缝与首行完整.md)

---

## AICFG-20260922-002 oasis-ocr-sampler-calibrate（第 12 次更新）

- 资产类型：技能
- 归档绝对路径：`D:\知识库\和平精英绿洲起源\工具\AI配置归档\skills\workbuddy\oasis-ocr-sampler-calibrate\SKILL.md`
- 来源绝对路径：`C:\Users\Administrator\.workbuddy\skills\oasis-ocr-sampler-calibrate\SKILL.md`
- 部署目标绝对路径：`C:\Users\Administrator\.workbuddy\skills\oasis-ocr-sampler-calibrate\SKILL.md`
- 关联实体：技能 `oasis-ocr-sampler-calibrate`；函数 `detect_row_bounds` / `grid_drift` / `align_to_grid`；新增触发词「对齐到红线不准」「滑动到红线识别也不准」「地图顶部滑上去超出屏幕了/滑出屏幕」「报顶行已对齐但实际没对齐」「顶行贴红线/红线对齐」「对齐误判」
- 用途：修正「顶行贴红线」对齐的误判 —— 把「页面背景空档 + 3px 暗色分隔线 + 又一截背景空档」被合并成的 **11px 假缝**（其下沿恰好等于真实卡顶）识别为 `top_in_gap` 小正值；同时**删除被三重证据证伪的 `scrolled_past` 状态与 `implied_top` 反推**，使 `drift` 恒 ≥ 0。
- 调用入口：WorkBuddy 技能自动加载；如需手工替换，直接覆盖 `C:\Users\Administrator\.workbuddy\skills\oasis-ocr-sampler-calibrate\SKILL.md` 后重启客户端会话。
- 输入 / 输出：输入为「用户口头描述的失败现象 + 真实截图/真机帧」；输出为修正后的技能铁律（25 条扩展为 8 个子项，含逐像素剖面数据、三条判据代码、被证伪方案记录、排查方法论）。
- 依赖：无新增依赖。
- 版本：`metadata.version` 未标注（沿用原值）
- 证据状态：项目实测 —— 逐像素剖面（`zoom_A_180_420.png`）+ 放大目视（`onecard_141831.png`）+ 合成帧回归（N2 16 档）+ 全量 43 帧扫描（负 drift 帧 = 无）+ 验收 10/10 断言
- 生命周期：可复用；清理条件：不清理
- 更新时间：2026-09-22

**校验**：字节数 **50,393 B**，SHA256 `8E5C81440E1595140D48504569F23A9F678AD7826CB07FC2E9304D7B9B2B5722`（归档副本与客户端副本逐字节一致）。
**体积序列**：15,007 → 17,687 → 17,728 → 23,794 → 26,351 → 28,152 → 30,621 → 31,706 → 39,495 → 43,x → **50,393**。

---

## 本次改动摘要

铁律 25 从「四条实测结论（2026-09-21）」重写为「两条可证判据 + 一条证伪记录 + 排查方法论」：

| 子项 | 变化 |
| --- | --- |
| 锚点是行间亮缝 | 保留；补记「假缝」成因 |
| `spacing_ok` 闸门 | 🔴 **订正**：不再「不符就停」，改为记录 + 继续补滚（缝可能落在封面浅色平坦区） |
| 假缝成因（新增） | 逐像素剖面数据 + 「用户说的『地图顶部滑上去了』其实是把背景/分隔线误当成地图卡」 |
| `scrolled_past` / `implied_top`（新增·证伪） | 🔴 **三重证据证伪，明确写「不要再实现」**；判据只留三条，`drift` 恒 ≥ 0 |
| cut 过冲保护 | 保留；明确它与被删除的「负 drift 反向回滚」是两回事 |
| 位移测量带方向 / 触控 slop | 保留 |
| 文案口径（新增） | 「偏差 >0 表示整行仍偏在红线下方」 |
| 排查方法论（新增） | **用户对现象的描述可能是错的，必须自己三重取证，不要照描述直接改代码** |

同步更新：`name` / `description` 追加 6 个新触发词；验证章节重写 N 段说明（N1 / N1b / N1c / N1d / N2 / N3）并新增一条「给对齐逻辑做验证必须带『全量扫描 + 负值禁令』断言」的忠告。
