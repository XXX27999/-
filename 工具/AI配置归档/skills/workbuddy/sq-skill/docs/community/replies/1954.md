# 帖子 1954 的回复

类型: 0
第 1 页 / 共 1 页，共 3 条

1. 回复ID: 5738
   - 作者: 哈利·波特 (5948)
   - 时间: 2026-03-30 10:48:30
   - 正文: 确认几个问题
1. MainUI这个控件是否确实正常创建了（可以通过在Contrust函数中增加ugcprint接口去打印信息来验证）
2. MainUI若成功创建在玩家视口中，确认一下这个脚本是否有报错，若有报错也可能不执行后续逻辑
3. 建议在按钮点击事件函数中也增加对应的日志打印以确保这个按钮点击之后确实触发了这个函数的执行
4. 最后一点，SendSystemMessageToAll，这个函数只能在服务端调用，UI里面的逻辑都是在客户端执行的，若需要调用这个接口需要通过RPC在服务端执行。
   1.1 子回复ID: 5755
      - 作者: 蓝方团队 (6304)
      - 时间: 2026-03-31 21:23:16
      - 正文: 在Contrust函数中增加
if self.bInitDoOnce then
return;
end
self.bInitDoOnce = true;
后就可以正常监听并执行了
   1.2 子回复ID: 5754
      - 作者: 蓝方团队 (6304)
      - 时间: 2026-03-31 21:21:23
      - 正文: 1.验证成功
2.没有报错
3.点击事件函数中打印日志未成功
4.实际调试时已经删除了此函数
我想请问一下
if self.bInitDoOnce then
return;
end
self.bInitDoOnce = true;
中的self.bInitDoOnce是什么意思？
